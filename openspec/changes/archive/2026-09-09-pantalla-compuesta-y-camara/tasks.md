## 1. La dependencia

- [x] 1.1 Anotar la línea de partida del bundle ejecutando `npm run build` **antes de tocar nada**, y comprobar que coincide con la de `proposal.md` §Impact (principal 625,00 kB, `GameScene` 830,75, 221 módulos); si no coincide, decirlo antes de seguir
- [x] 1.2 Comprobar con `npm ls three` que hoy hay **una sola** copia en 0.170.0, y guardar la salida para compararla después
- [x] 1.3 Instalar `@react-three/drei@^9.122` en el workspace (`npm install -w @codeplay/web`) y verificar que `npm ls three` **sigue dando una sola copia en 0.170.0**
- [x] 1.4 Leer el diff de `package-lock.json` y verificar que sólo añade lo de `drei`: si los `@supabase/cli-linux-*` han desaparecido, **restaurar el lockfile** y parar — el CI corre `npm ci` sobre ubuntu

## 2. La cuenta de los pasos dados, fuera del componente

- [x] 2.1 Añadir en `apps/web/src/game/interpreter.ts`, junto a `countSteps`, la función pura que da los pasos dados —cero sin intento, `index + 1` mientras corre, `index` al terminar o al congelar— con su comentario de por qué vive fuera del componente
- [x] 2.2 Añadir su bloque en `interpreter.test.ts` cubriendo los cinco estados —reposo, primer paso, a mitad, detenido y terminado— y verificar que pasan con `npm run test:run`
- [x] 2.3 Añadir el test que ata el número final a `countSteps(orders)` —el que hoy no puede fallar y fija la construcción— y verificar que pasa

## 3. La caja de bloques, separada del lienzo

- [x] 3.1 En `apps/web/src/game/blocks.ts`, dejar la lista de bloques que muestra el flyout en lugar del `categoryToolbox`, y verificar que `npm run test:run` sigue pasando (`blocks.test.ts` no depende de la caja)
- [x] 3.2 En `BlockEditor.tsx`, aceptar el hueco de la caja como dato y, cuando lo haya, inyectar el espacio **sin toolbox** y crear el flyout suelto (`createDom` → `appendChild` → `init` → `setAutoClose(false)` → `show`), verificando en el navegador que los tres bloques se ven en la columna derecha
- [x] 3.3 Sobrescribir `getOriginOffsetInPixels` en el espacio del flyout, con su comentario de por qué, y verificar arrastrando un bloque que **cae bajo el cursor** y no en el origen del lienzo
- [x] 3.4 Declararle el lado real con `toolboxPosition`, y verificar soltando un bloque en el lienzo que **no se borra**; comprobar también que arrastrar un bloque del lienzo **a la caja** sí lo tira
- [x] 3.5 Deshacer el flyout en la limpieza del efecto, y verificar recargando la pantalla en desarrollo que no quedan dos cajas ni errores en consola (`React.StrictMode` monta dos veces)
- [x] 3.6 En `BlockEditorLoader.tsx`, pasar el hueco hacia abajo, y verificar leyendo el archivo que **sigue sin importar `blockly`**

## 4. La escena: cámara, detener, contador y superposición

- [x] 4.1 Montar `OrbitControls` en `GameScene.tsx` con los topes de giro y acercamiento, sin desplazamiento y sin poder mirar desde abajo, y verificar en el navegador **moviendo la cámara** e intentando pasarse de cada tope. Topes de giro y de rueda comprobados intentándolos, y **el desplazamiento probado con el botón derecho**: la cámara se quedó en (3,80 / 5,40 / 5,40) **sin mover un decimal**, mientras el mismo arrastre con el izquierdo la llevó a (−6,60 / 5,40 / −0,29) conservando la distancia al centro (8,5299 → 8,53): gira alrededor del tablero y no lo desplaza
- [x] 4.2 Añadir el control de volver a la vista inicial, superpuesto en la esquina libre del juego, y verificar que tras girar y acercar devuelve exactamente la vista de partida
- [x] 4.3 Añadir «detener» —adelantar el índice y marcar el intento como congelado—, y verificar en el navegador que el personaje se queda en la casilla del paso en curso y **no** vuelve a la salida. Comprobado contra la **posición real del objeto en la escena**: a mitad del quinto paso el personaje estaba en col 3,3176 —entre dos casillas— y al detener quedó en **col 4, fila 4, mirando al este**, que es exactamente `steps[4].pose`, la pose del paso en curso. **No volvió a la salida** (col 0) y **aterrizó en la casilla**, no entre dos. Un segundo después, igual
- [x] 4.4 Comprobar que «Ejecutar» tras un recorrido detenido empieza **desde la salida** y que «Detener» sólo se puede pulsar con una ejecución en curso. Comprobado **en el tablero**: detenido en col 4, al pulsar «Ejecutar» el personaje está a los 40 ms en **col 0, fila 4** —la salida— con el contador en 1. Y «Detener» inhabilitado sin ejecución en curso, medido
- [x] 4.5 Rehacer el contador con la función del punto 2: siempre visible, «Pasos: 0» en reposo, y verificar en el navegador los cuatro estados —en particular que un recorrido de 10 pasos **termina en 10 y no en 11**—
- [x] 4.6 Mover el resultado, los dos avisos de bloques sueltos y el texto de reposo a la superposición sobre el juego, **sin reescribir ni un texto**, y quitar la franja de controles
- [x] 4.7 Verificar con el resultado en pantalla que **el personaje y la casilla en la que ha quedado se siguen viendo**; si estorba, preguntar antes de rediseñar
- [x] 4.8 Pintar los tres botones con `createPortal` en el hueco que baja la composición, y verificar en el navegador que funcionan desde la columna derecha (con `.click()` desde JavaScript: los clics del panel no los disparan)
- [x] 4.9 En `GameSceneLoader.tsx`, pasar el hueco de los controles hacia abajo, y verificar leyendo el archivo que **sigue sin importar `three`**

## 5. La composición

- [ ] 5.1 Recomponer `StudentGameLabModule.tsx` en las zonas del boceto, y verificar en el navegador que la pantalla cabe sin desplazamiento y que cada cosa está donde el boceto la pone. **HECHA A MEDIAS, y se deja sin marcar.** El boceto se cumple —verificado zona a zona en el punto 8— y **la maqueta sí cabe**: el marco acaba a **877 px** del principio del documento a 1440 de ancho y a **920** a 1024, contra los 916/959 de la primera versión. Lo que sigue sin caber es **el laboratorio entero**: el documento mide **1244 px** (1312 a 1024), porque debajo van el `<pre>` y su tarjeta. **El alto de ventana que pide la maqueta es 880 px a 1440 y 920 a 1024**; para ver además el `<pre>` hacen falta 1250. Lo que sobra es del laboratorio y no de la maqueta que hereda el J8
- [x] 5.2 Crear los dos huecos en estado (no en `ref`) y bajarlos a los dos cargadores, y verificar que la caja y los botones aparecen en la columna derecha al cargar la pantalla de cero
- [x] 5.3 Escribir el panel de instrucciones de relleno, diciendo de sí mismo que es un ejemplo y que el texto real llega del nivel en el J8
- [x] 5.4 Dejar el `<pre>` del programa **debajo** de las cuatro zonas, y verificar que sigue enseñando el sobre

## 6. Verificación en el navegador, completa

- [x] 6.1 **Antes de nada, mirar el `<pre>`**: si no contiene `codeplay_advance` después de cargar un programa, decirlo y no verificar encima (§4.10). Y contar un frame de `requestAnimationFrame`, que es la señal: **cero frames** significa que Blockly no reparte **y** que la escena tampoco avanza; **frames que corren** descartan lo de Blockly pero **no** lo de la escena —sesenta fps con el recorrido congelado es el caso medido en el J6, y ahí la señal es que el recorrido progrese—. **`document.hidden` no vale para ninguno de los dos**: sale `false` en ambos
- [x] 6.2 Emular el viewport con `resize_window` —sin ello la escena 3D no avanza, y contar frames **no** lo detecta— y jugar el PROGRAMA A entero: contador de 0 a 10, resultado con su frase, y la consola sin un solo error
- [x] 6.3 Verificar el arrastre desde la caja separada con eventos de puntero reales, comprobando el `<pre>` después: el programa tiene que llegar a la aplicación
- [x] 6.4 Verificar los dos avisos de bloques sueltos en su sitio nuevo: el de mientras se construye y el del resultado, con la regla de cuál manda intacta
- [x] 6.5 Verificar «Reiniciar» a mitad —contador a cero, superposición al texto de reposo— y «Detener» —contador congelado, sin resultado—. «Reiniciar» probado **con el recorrido vivo**: con el personaje en col 3,2721 —entre dos casillas— volvió a **col 0, fila 4, mirando al norte**, contador a 0 y banda al texto de reposo. Vuelve a la salida exacta, no a medio camino, que es lo que el bucle de frames tenía que replantar. «Detener» también

## 7. Medidas, comprobaciones y documentación

- [x] 7.1 Ejecutar `npm run build` y anotar los cuatro trozos y los módulos contra la línea de partida: `GameScene` sube por `drei`, **el principal no se mueve**
- [x] 7.2 Comprobar con marcas que sobreviven a la minificación —`rootCount` y los textos de la barra, nunca nombres de función— que el intérprete **no ha subido** al trozo principal
- [x] 7.3 Ejecutar `npm run lint` y `npm run test:run`, y verificar que los dos pasan sin avisos
- [x] 7.4 Actualizar `docs/CONTEXT.md` §2.9 —la composición, la vía del flyout suelto con sus tres apoyos, la cuenta rehecha y qué se decidió sobre detener— y §4.8 con las medidas nuevas
- [x] 7.5 Actualizar `docs/CONTEXT.md` §1.2 y `openspec/config.yaml` con la entrada de `drei` en el stack, y verificar con `npx openspec doctor` que el YAML sigue parseando
- [x] 7.6 Anotar en `docs/CONTEXT.md` §4.10 y §2.9 la causa encontrada de que el editor deje de publicar —el `requestAnimationFrame` de `fireInternal`, con su línea—, que el `rAF` sólo se programa con la cola vacía, y que el estrangulamiento **no fue el mismo en las dos máquinas**: es documentación, no requisito, y quien decide qué hacer con §4.10 es el usuario
- [x] 7.7 Poner el J6.3 en ✅ en `docs/ROADMAP-JUEGO.md` §3
- [ ] 7.8 Enumerar las rutas en `git add` —nunca `git add -A`— y comprobar con `git status` que no se cuela nada de otro cambio vivo antes de commitear

## 8. Los seis defectos que el usuario vio con el paso implementado

Sin commitear, así que se corrigen **dentro de este cambio**: no hay propuesta
nueva. Cada punto se verifica **mirando dónde está el bloque o el personaje**, no
leyendo lo que la pantalla dice de sí misma.

- [x] 8.1 Que el bloque arrastrado se vea por encima de todo, incluido el `<canvas>` 3D: apagar los recortes mientras dura el arrastre desde `BlockEditor.tsx`, con la regla en `main.css`. **Verificado mirando dónde se dibuja**: soltándolo a mitad de camino sobre el juego, el bloque está en (670, 407) —el lienzo empieza en y 657— y `elementFromPoint` en su centro devuelve **el propio bloque**, no el lienzo ni el `<canvas>`. Los cuatro recortes en `visible` durante el arrastre, medido
- [x] 8.2 Rehacer entera la zona de borrado: la caja deja de serlo (`getClientRect` a `null`) y soltar fuera del lienzo devuelve el bloque a la caja, con el vuelo a la vista. **Verificado viéndolo volver**: seis muestras del vuelo, de (622,417) a (978,330), camino de la caja en (1084,279); y después el `<pre>` vuelve a `"workspace": {}`. Soltado sobre el juego y sobre el panel de instrucciones, los dos vuelven. Soltado **dentro** del lienzo se queda —comprobado moviéndolo dos veces dentro—, y arrastrado **a la caja** se tira
- [x] 8.3 La maqueta sin tarjetas apiladas. Se implementó primero como **un solo rectángulo con líneas finas**, que es lo que el usuario pidió al devolver la primera versión; el **9-sep-2026 la fijó con una imagen** en su forma final: **paneles flotando sobre el juego**, sin bordes gruesos, con iconos en los rótulos, «Detener» en gris y los dos giros en colores distintos. Verificado midiendo las zonas (punto 8.7)
- [x] 8.4 Fundir juego y lienzo: el juego pasa a una zona de todo el alto y el lienzo va superpuesto en su parte baja como bandeja. **Y reencuadrar el tablero, que es lo que la bandeja rompía**: medido, la esquina sureste caía 154 px por debajo del borde de la bandeja. Con la cámara a 11,86 y el tablero levantado 2,4 el tablero entero queda en 258-591, con la bandeja empezando en 614
- [x] 8.5 Quitar la barra de desplazamiento de la caja: `flyoutWorkspace.scrollbar` destruido tras `init`. Verificado: **cero** elementos `.blocklyFlyoutScrollbar` y los tres bloques a la vista —de y 97 a y 241 en una caja de 200 px—
- [x] 8.6 Mover la banda del mensaje a la franja del título del lienzo, con la etiqueta fija a la izquierda, y sin reescribir ni un texto. Verificado los cuatro estados en su sitio nuevo: reposo, «Ejecutando…», el resultado y los dos avisos de sueltos
- [x] 8.7 Medir las zonas y compararlas con el boceto. Marco 1123×640; juego 0,2-71,3 % de ancho y 0,4-99,6 de alto; bandeja 58,9-97,1 % de alto dentro de él; caja 6,6-37,9 %; botones 37,9-48,1 %; instrucciones 48,1-99,6 %. Los tres botones en fila, 90 px cada uno
- [x] 8.8 Comprobar que lo medido del J6.3 sigue en pie **contra el tablero**: PROGRAMA A de col 0/fila 4 a col 4/fila 0 con el contador de 1 a 10 y «¡Perfecto! …con 10 pasos»; «Detener» a mitad congela en col 4/fila 4 mirando al este con «Pasos: 5» y sin resultado, y sigue igual un segundo después; «Ejecutar» después empieza en col 0/fila 4 con el contador en 1; «Reiniciar» devuelve a col 0/fila 4 mirando al norte con «Pasos: 0»
- [x] 8.9 Comprobar la cámara con los topes: acercamiento 4 y 14 exactos, giro 25,71° y 83,12° exactos —nunca por debajo del tablero—, azimut libre y «Vista inicial» devolviendo a (5,3 / 7,5 / 7,5). **Con una salvedad medida**: pulsada a los 500 ms de soltar un giro largo, la inercia de los controles sigue corriendo después del reinicio y deja el azimut a 45,4° en vez de 35,2°; esperando a que la inercia pare, vuelve exacto. Es de la amortiguación de `drei`, que este paso no toca
- [x] 8.10 Comprobar que el personaje no queda tapado con el resultado en pantalla: sobre la meta proyecta en (883, 402), la bandeja empieza en 614, y `elementFromPoint` ahí devuelve el `<canvas>`
- [x] 8.11 Rehacer las medidas del trozo principal y las marcas de frontera después de los seis arreglos
- [x] 8.12 Actualizar el spec, el `design.md` —con la decisión de borrado rehecha entera— y `docs/CONTEXT.md` §2.9 y §4.8
- [x] 8.13 El aspecto de la imagen del 9-sep-2026: píldoras oscuras para «Vista inicial» y «Pasos», iconos en los tres rótulos y en los botones, «Detener» en gris, los dos giros con colores distintos, y el lienzo vacío con su marco discontinuo y su texto. **El hueco de la mascota se deja vacío**, como manda el repo
- [x] 8.15 Las correcciones del 9-sep-2026 sobre la forma nueva: los dos apartados de la derecha **pegados** en un panel con línea de separación y el recuadro de instrucciones creciendo para no dejar blanco; **fuera el cuadro gris del flyout** —el tercero de los fondos de Blockly que hay que apagar—; **bloques con el renderizador `zelos`**, tema con la tipografía del proyecto e icono por bloque en `data:`, verificado que `blocks.test.ts` sigue pasando —el JSON del contrato no cambia—; **la caja deja de heredar la altura del lienzo** (`getHeight`), que era la deuda que el propio `design.md` dejó anotada, porque con bloques de 56 px el tercero se cortaba —medido: los tres caben ahora, de y 305 a y 521 en una caja que acaba en 537—; **fuera la barra horizontal** del lienzo y la vertical **fuera del cuadro**; y el amarillo del cuadro pasa al mismo lavanda de la caja y de «Reiniciar»
- [x] 8.16 La segunda tanda del 9-sep-2026: **cielo** del juego en degradado azul suave a casi blanco —puesto en la zona, porque el `<canvas>` es transparente—; **escala fría** con cuatro nombres nuevos del tema, escritos también como utilidades a mano porque un color nuevo en la configuración de Tailwind no aparece hasta reiniciar el servidor; **el lienzo empieza arriba** (`scrollbar.setY(0)` al montar y al cambiar de tamaño), verificado: la barra arranca en y 657 con el cuadro en 656; y **el texto de los bloques ya no desaparece al pasarles el ratón** — la causa era que el CSS de `zelos` va con ámbito al `injectionDiv` y la caja vive fuera, así que se le añaden al hueco las clases del renderizador y del tema. Comprobado con eventos sintéticos de puntero: antes y con el ratón encima, el texto sigue en `rgb(255,255,255)`, visible, opacidad 1, sin filtro y del mismo ancho
- [x] 8.17 La tercera tanda del 9-sep-2026: **cielo de verdad** —degradado de tres paradas, del azul claro al casi blanco—; **el lienzo se pliega y se estira**, sin desmontar el editor y sin animar el alto, verificado que los bloques sobreviven al pliegue —2 bloques, bandeja de 258 a 68 y vuelta, los dos visibles dentro del lienzo al abrir— y que el tirador va de 190 a 286 estirando y topa en 90 encogiendo; y **el lienzo empieza arriba de verdad**, con `scroll(0, 0)` en vez de `scrollbar.setY(0)`, que dejaba el contenido fuera de vista por abajo —medido: los bloques caían en y 853 y 993 con el lienzo acabando en 845—
- [x] 8.18 **El fallo de «pegar dos bloques y separarlos deja la pantalla en blanco», CERRADO con la traza del usuario.** `Cannot read properties of null (reading 'type')` en `readOrder` ← `readProgram` ← `hasLooseStacks` ← `GameScene`: Blockly serializa `next: { block: null }` mientras se arrastra el bloque de abajo, `block.next?.block` devolvía ese `null` y el bucle seguía porque `null !== undefined`. Y como `hasLooseStacks` corre en el pintado de la escena, la excepción tumbaba el árbol de React entero. Arreglado parando la cadena cuando lo que sigue no es un bloque, con **test que lleva el `null` literal y que muerde** —comprobado revirtiendo la línea: falla con ese mismo `TypeError`—. De paso quedan blindados el cierre del contador del retorno (en `finally`, o un `dispose` que lance deja la clase de los recortes puesta para siempre) y las dos llamadas de captura de puntero del tirador
- [x] 8.19 Lo que NO reprodujo el arrastre sintético: cinco variantes —separar dentro, separar fuera, arrastrar la cadena entera, unir dos bloques— sin un solo error, porque **unir dos bloques con eventos sintéticos no llega a encajar la conexión** y ése era justo el camino. Lo cerró la traza de consola, no la reproducción Probadas cinco variantes con eventos de puntero sintéticos —separar dentro del lienzo, separar y soltar fuera, arrastrar el bloque de arriba con su cadena colgando, y unir dos bloques arrastrando— y en ninguna hubo error ni se rompió la página; unir dos bloques con eventos sintéticos no llega a encajar la conexión, así que **el camino exacto del usuario no se ha podido recorrer**. Lo que sí se corrigió, porque era un fallo real de este código en ese mismo camino: el retorno del bloque a la caja cerraba el contador **después** de `dispose`, así que un bloque ya eliminado dejaba la clase de los recortes puesta para siempre —bandeja por encima de todo y sin recortar, sin forma de volver sin recargar—; ahora el cierre va en `finally` y el `dispose` comprueba antes que el bloque siga vivo. **Falta el mensaje de consola del usuario para cerrarlo**
- [ ] 8.14 **Volver a ver un arrastre real de punta a punta con la forma nueva.** Al cambiar la maqueta a paneles flotantes la página entró en el estado de §4.10 y se quedó ahí —**cero frames de `requestAnimationFrame` en 700 ms, cero eventos al crear un bloque y el `<pre>` en `{}` con cuatro bloques pintados**—, así que el arrastre sale mal por eso y no por el código, y nada medido encima vale. Lo que sí quedó comprobado: la maqueta medida zona a zona; que el bloque **cae bajo el cursor** con el renderizador nuevo y la caja desacoplada —soltado en (464, 533), el bloque queda en (430, 511), que es el agarre—; y que la regla de los recortes **nombra los elementos correctos** —encendiendo la clase a mano, los cinco pasan a `overflow: visible` y la bandeja a `z-index: 30`—. **Hay que verificarlo en otra sesión**
