## 1. Migración: las dos funciones del canje

- [x] 1.1 Escribir `supabase/migrations/202606030022_create_invitation_redemption.sql` con `redeem_invitation(input_token text) returns public.class_memberships`, copiando el orden de `accept_join_request` de la 0013: **`set search_path = public` en la declaración** —sin ella, quien llama antepone un esquema propio y `profiles` y `class_groups` resuelven a tablas suyas mientras el cuerpo corre con los privilegios del dueño, así que la comprobación de rol y la de pertenencia se esquivan las dos—; `auth.uid()` leído dentro y nunca como parámetro; rol `child` comprobado contra `profiles`; invitación buscada por token; `for update` sobre `class_groups` **antes** del `count(*)` de alumnos; cupo; «un alumno, un salón»; `delete` de la solicitud **pendiente** de quien canjea con el `where status = 'pending'` escrito; `insert` en `class_memberships`; y `update` de la invitación a `accepted` con `accepted_at`. Verificar leyendo el archivo que no queda ninguna escritura fuera del cuerpo de la función, que es una sola transacción
- [x] 1.2 En la misma migración, levantar los tres motivos distinguibles con los códigos del design §3 —`ZC010` inexistente, `ZC011` caducada, `ZC012` ya canjeada—, la caducidad comparada contra `timezone('utc', now())` y no contra la columna `status`, y `42501` / `23514` / `23505` para sesión-o-rol, cupo y pertenencia previa. Verificar que **los once** `raise exception` del archivo llevan su `using errcode` (ocho en el canje, tres en la previsualización); la propuesta decía «seis» y el recuento estaba mal
- [x] 1.3 En la misma migración, añadir `preview_invitation(input_token text)` como función `stable` `security definer` **con `set search_path = public`**, por el mismo motivo que 1.1 y con la misma exigencia de escribirlo y no arrastrarlo, que devuelve el nombre del salón, su ID público, los cupos libres y el estado calculado (`valid` / `used` / `expired`) **sin escribir nada**, levantando `ZC010` sólo si el token no existe. Verificar leyendo el cuerpo que no contiene ni un `insert`, ni un `update`, ni un `delete`
- [x] 1.4 Cerrar los permisos de las dos funciones: `revoke execute ... from public` y `from anon` **por separado** —revocar de `public` no retira lo concedido a un rol, ver la 0009— y `grant execute ... to authenticated`. Verificar que ninguna de las dos se concede a `anon`, incluida `preview_invitation`
- [x] 1.5 Comprobar con `grep -c "search_path"` sobre el archivo que las **dos** funciones declaran `set search_path = public`: las nueve `security definer` de las migraciones lo llevan sin excepción, y es lo que la revisión echó en falta en esta propuesta
- [x] 1.6 Comprobar que la migración **no** crea tablas ni columnas, **no** toca ninguna política ni ningún `grant` de `invitations`, y **no** modifica `accept_join_request`: `grep -n "alter table\|create policy\|drop policy\|grant \|revoke " ` sobre el archivo debe devolver únicamente las **seis** líneas de permisos de la tarea 1.4 —dos `revoke` y un `grant` por función—; la propuesta decía «cuatro» y el recuento estaba mal
- [x] 1.7 Documentar la 0022 en `supabase/README.md` con el formato de las entradas anteriores, diciendo que no cambia el esquema de ninguna tabla y que sí exige regenerar los tipos porque añade dos funciones que el cliente llama por `rpc()`
- [ ] 1.8 **PARADA. No la marca esta sesión.** El `db push` lo lanza el usuario, y no antes de que la sesión que revisa haya leído el SQL y lo haya dicho (`ROADMAP.md` §1.3, punto 9). Al lanzarlo se lee la salida: tiene que aplicar la 0022 y ninguna otra. Después, `npx supabase gen types typescript --linked` para que las dos funciones entren en `apps/web/src/types/database.types.ts`, que **no se edita a mano**. Verificar que el diff de ese archivo contiene `redeem_invitation` y `preview_invitation` y ninguna tabla nueva

## 2. Servicios

- [x] 2.1 Crear `apps/web/src/services/invitations.service.ts` con la interfaz tipada y la forma `{ data, error }` de `AppError`, sin lanzar nunca: `createInvitation(groupId, tutorId)`, `listInvitations(groupIds)`, `deleteInvitation(id)`, `purgeExpired(groupIds)` y `previewInvitation(token)`. `invited_by` se manda **explícito** —no tiene `default auth.uid()`, y omitirlo responde `42501`, que se diagnostica mal—. Verificar con `npx tsc` (vía `npm run build`) que ningún método devuelve `any`
- [x] 2.2 En ese servicio, su **propio** mapa `ERROR_MESSAGES` con los textos mirando al niño, no al tutor: `23514` «Ese salón ya está lleno», `23505` «Ya perteneces a un salón», más `ZC010`, `ZC011`, `ZC012` y `42501`. Verificar que `classrooms.service.ts` **no** se toca para esto: sus mensajes de `23514` y `23505` están redactados para el tutor y se quedan como están
- [x] 2.3 Añadir `redeemInvitation(token)` a `ClassroomsService` en `apps/web/src/services/classrooms.service.ts`, llamando a la RPC `redeem_invitation` igual que `acceptRequest` llama a `accept_join_request`, y traduciendo con el mapa del niño de la tarea 2.2 y no con el suyo. Verificar que sigue devolviendo `{ data, error }` y que no lanza
- [x] 2.4 Añadir el doble de ese método en `apps/web/src/test/fakeClassroomsService.ts`, que es el coste declarado en el design §6. Verificar que `npm run test:run` sigue pasando sin tocar ningún test existente

## 3. Store de salones y hook del tutor

- [x] 3.1 Añadir la acción `redeemInvitation` a `apps/web/src/context/ClassroomsContext.ts` y a `ClassroomsProvider.tsx`, pasando por `runWrite` como las demás escrituras para que la pertenencia recargue en el acto. Verificar que **no** se añade ningún acceso a `supabase` dentro del provider: sigue hablando sólo con el servicio
- [x] 3.2 Crear `apps/web/src/hooks/useInvitations.ts` con la carga, el error y las acciones de los enlaces del tutor, siguiendo la forma de `useMissionAssignments` —`runLoad(silent)` que apaga siempre la espera, y recarga tras cada escritura—. **No toca `ClassroomsProvider`**, por el mismo criterio que las misiones en el paso 16. Verificar que compila y que el hook no importa `ClassroomsContext`
- [x] 3.3 En ese hook, purgar antes de listar: borrar las invitaciones caducadas de los salones consultados con la política de borrado que ya existe, sin función nueva ni permiso nuevo (design §10). Verificar que el borrado usa `expires_at` y no `status`
- [x] 3.4 Derivar el estado de cada enlace de **dos** datos —`status` y `expires_at`—, con las tres salidas del design §9. Verificar por lectura del código que no queda ningún ternario de dos ramas sobre `status`: es el aviso vivo de `ROADMAP.md` §3

## 4. El token que sobrevive al registro

- [x] 4.1 Crear `apps/web/src/context/invitationToken.helpers.ts` copiando `oauthRole.helpers.ts`: `savePendingInvitationToken`, `clearPendingInvitationToken` y `takePendingInvitationToken`, **que lee y borra en la misma llamada**. Verificar que no se exporta ninguna función que sólo lea sin borrar
- [x] 4.2 Crear `apps/web/src/context/invitationToken.helpers.test.ts` a imagen de `oauthRole.helpers.test.ts`, con el caso que da sentido al patrón: tras una lectura, una segunda lectura devuelve `null`, de modo que la invitación no alcanza a la persona siguiente del mismo navegador. Verificar que ese test **falla** si se parte la función en un `get` y un `clear` separados
- [x] 4.3 Consumir el token en `apps/web/src/hooks/useRoleHomeRedirect.ts`: si hay token guardado, el destino tras autenticarse es `/invite/<token>` en vez de `getHomeRouteForRole(user.role)`. Verificar que `useRoleHomeRedirect.test.tsx` **sigue pasando sin tocarlo**: sin token el hook tiene que hacer exactamente lo de hoy
- [x] 4.4 Consumir el token en `apps/web/src/pages/AuthCallback/AuthCallback.tsx`, **después** de resolver el rol y en los dos desenlaces que dejan a la persona dentro (rol fijado y rol bloqueado). Verificar que el camino sin token navega igual que antes

## 5. Rutas y pantallas

- [x] 5.1 Añadir `INVITE: '/invite'` a `apps/web/src/constants/routes.ts` y la ruta `/invite/:token` a `apps/web/src/router/AppRouter.tsx` **sin guarda**, con el comentario que diga por qué —una guarda tiraría el token de quien llega sin cuenta, que es el caso normal—. Verificar que `PrivateRoute.tsx` no se ha tocado. **`PublicRoute.tsx` SÍ se toca**, y no por la ruta —que no lleva guarda— sino por el resolutor de aterrizaje: la verificación en navegador demostró que esa guarda decide destino y **gana siempre** al hook, así que el enlace no llegaba nunca. Ver el apartado nuevo de `design.md`
- [x] 5.2 Crear `apps/web/src/pages/Invite/Invite.tsx` resolviendo sus seis estados: sin sesión (guarda el token y ofrece registrarse o entrar), sesión de niño con enlace válido (nombre del salón y botón de entrar), sesión de tutor, enlace caducado, enlace ya usado y enlace inexistente. **El canje no ocurre al cargar**, sólo al pulsar. Verificar en el navegador que abrir la pantalla y salir sin pulsar deja el enlace utilizable
- [x] 5.3 Vestir la pantalla con los tokens del tema selva y los adornos de `components/decor/`, tomando `AuthCallback.tsx` como referencia de composición. Verificar que no aparece ningún hex suelto: los colores salen de los nombres del tema
- [x] 5.4 Reescribir `apps/web/src/components/dashboard/teacher/AddStudentsPanel.tsx`: conservar el bloque del ID público con sus tres pasos, retirar el párrafo «Todavía no enviamos correos de invitación» —deja de ser la única verdad de esa pantalla—, y añadir generar el enlace, copiarlo al portapapeles y la lista de enlaces con sus tres estados y su acción de retirar. Verificar en el navegador que el panel sigue mostrando el ID público y los cupos libres
- [x] 5.5 Distinguir los dos rechazos por pertenencia previa, que el servidor no distingue: comparar el ID público que devuelve `preview_invitation` con el del salón del niño (`currentGroup.publicId` de `useClassrooms()`). Si es el suyo, «ya estás en este salón» y ningún botón; si es otro, «sal de tu salón antes de entrar en otro». **Sin migración**: el dato para distinguirlo ya está en el cliente. Verificar los dos casos en el navegador
- [x] 5.7 El registro que ofrece el enlace lleva el tipo de cuenta **ya elegido**: ruta `${ROUTES.SIGNUP}/:role`, `Signup` la lee con `useParams` y arranca en el formulario, y la pantalla del enlace apunta a `ROUTES.SIGNUP_CHILD` —constante que existía en `routes.ts` sin ninguna ruta detrás—. **Sin el botón de cambiar tipo cuando el rol vino en la dirección**: volver al selector desde una invitación es el error que saltárselo viene a evitar, y ese error no tiene arreglo porque el rol se fija en el primer registro (0018). Verificado en el navegador: `/signup/child` va directo al formulario con el distintivo «Explorador», `/signup` sigue empezando por el selector y `/signup/superadmin` cae en él
- [x] 5.6 Mostrar el error del servicio en la propia pantalla desde la que se intentó, en las dos superficies nuevas, como manda la convención (`shared/StoreErrorNotice.tsx` o el patrón equivalente). Verificar provocando un fallo real, no razonándolo

## 6. Verificación contra la base real

- [x] 6.1 Escenario: salón `CP-PJE6` «salon pinpon» del tutor 1, cupo 30, cero miembros. Confirmar por `curl` con el token del tutor 1 que sigue con **cero** miembros antes de empezar: quien canjea tiene que estar fuera de todo salón, porque «un alumno, un salón» bloquea el canje si ya pertenece a uno
- [x] 6.2 Negativos rápidos por `curl` con la cuenta de `VITE_DEV_CHILD_*`: token inexistente → `ZC010`; token caducado → `ZC011`; token ya canjeado → `ZC012`; con la clave anónima → 401; desde la sesión del tutor 1 → `42501`. Comprobar además que un `select` directo sobre `invitations` desde el niño devuelve cero filas y que un `update` responde por falta de permiso: son los dos muros que obligan a la RPC
- [x] 6.3 Positivo emparejado con cada negativo, en la misma ventana: el canje bueno inscribe, marca la invitación `accepted` con su `accepted_at` y deja el salón con un miembro. Un `[]` sólo dice que no hay filas visibles; el positivo es lo que prueba que el negativo discrimina
- [x] 6.4 El cupo: dejar el salón lleno y canjear otro enlace. Comprobar que responde `23514`, que no se crea pertenencia y que **la invitación sigue sin canjear**, para que valga cuando haya un cupo libre
- [x] 6.5 La solicitud pendiente: con el niño en `pending` sobre un salón, canjear un enlace de **otro** salón. Comprobar que queda inscrito en el del enlace, que la solicitud pendiente **desapareció** y que ninguna solicitud ya resuelta suya cambió de estado ni de fecha. Repetir con un enlace del **mismo** salón de la solicitud
- [x] 6.6 El `returning` del `insert` de la generación: comprobar que el tutor recibe el token en la misma llamada que crea la invitación. Es la suposición señalada en Riesgos; si falla, replegarse a una segunda consulta por `id` y anotarlo
- [x] 6.7 La purga: dejar una invitación caducada, abrir el panel del tutor y comprobar que la fila **ya no existe** en la base, no sólo que no se pinta

## 7. Verificación por navegador, con la cuenta de Google

- [x] 7.1 **Planificada desde el principio, no dejada para el final.** La cuenta **axoluk** está dada de alta con Google y **no tiene contraseña**, así que `grant_type=password` no le vale y sólo se puede manejar por navegador. Es la única con la que se puede medir la parte difícil de este paso. Confirmar antes de nada que esa cuenta no pertenece a ningún salón
- [x] 7.2 La trampa: abrir el enlace **sin sesión**, pulsar «Continuar con Google», completar la vuelta por `/auth/callback` y comprobar que **el canje ocurre solo al terminar**, sin volver a abrir el enlace. Es lo único que prueba que el token sobrevive la salida de la aplicación; sin una cuenta de Google esa trampa no se prueba, se supone
- [x] 7.3 El descarte: con la invitación ya consumida, registrar o entrar con **otra** cuenta en el mismo navegador y comprobar que **no** entra a ningún salón. Es el daño concreto que el patrón de `oauthRole.helpers.ts` existe para evitar, agravado aquí porque metería a alguien en un salón ajeno
- [x] 7.4 El registro con correo y contraseña: cuenta nueva desde el enlace, y el canje al terminar. **La segunda mitad cambió de forma con el arreglo 5.7**: desde una invitación ya no hay selector ni «Cambiar tipo de usuario», así que registrarse como tutor **no es alcanzable por la interfaz**. Sigue siéndolo escribiendo `/signup/tutor` a mano con el token guardado, y ahí el servidor responde `42501` y la pantalla del enlace lo explica sin decir «no tienes permiso». Se anota con esa forma, o el spec describiría una pantalla que no existe
- [x] 7.5 Con dos sesiones a la vez —el tutor en el navegador y el niño por `curl`, que es lo que describe `CONTEXT.md` §5—: el tutor con su salón abierto ve entrar al niño que canjea, **sin recargar**, por la publicación de `class_memberships` de la 0021. Y su bandeja de solicitudes **no** enseña ninguna solicitud del canje, que es la diferencia entera con el ID público
- [x] 7.6 Retirar un enlace sin usar y comprobar en el navegador que después ya no vale

## 8. Documentación y cierre

- [x] 8.1 `docs/CONTEXT.md`: corregir la fila de P5 «Enlace de invitación canjeable», que **se equivoca en las dos cosas que dice** —ni «probablemente Edge Functions» ni la dependencia «Envío de correo»—, y moverla a aplicadas con las rutas reales. Dejar «Envío real de invitaciones» donde está, con su dependencia de servicio contratado
- [x] 8.2 `docs/CONTEXT.md`: §2.3 y §2.4 con la vía nueva y la transición nueva de la máquina de estados; §2.7 con la 0022, la decisión de la solicitud pendiente y la purga que pasa de obligación heredada a hecho; §3 para cerrar el aviso del ternario de dos ramas y anotar `pg_cron` como cabo del paso 27; y §5 con lo verificado y su fecha
- [x] 8.3 `docs/ROADMAP.md`: anotar en §2.1 **el motivo de partir el 19** —la mitad A no depende de contratar ni instalar nada, y además esquiva entera la decisión de privacidad de §3.4 porque sin correo no se almacena la dirección de nadie—, dejar la mitad B con su dependencia de servicio en §2.2 y en la fila del 19, y marcar la fila con el estado que corresponda a una mitad hecha
- [x] 8.4 `openspec/config.yaml`: replicar el estado, la convención de que `/auth/callback` **ya no es la única ruta sin guarda** y por qué, y la del token que se lee y se borra en la misma llamada. Verificar con `npx openspec doctor` que el YAML sigue parseando
- [x] 8.5 Comparar los tests **por el nombre de cada `it(`** y no por el total, con `git show HEAD:<ruta>` contra el archivo actual: los añadidos son legítimos y ninguno de los 97 de hoy debe haberse ido ni haber sido «arreglado»

## 9. Verificación final

- [x] 9.1 `npm run lint` con cero errores y cero warnings
- [x] 9.2 `npm run test:run` en verde, con el recuento nuevo anotado y justificado por nombres
- [x] 9.3 `npm run build` sin errores de TypeScript
- [ ] 9.4 Al preparar el commit, **enumerar las rutas** en `git add`, nunca `git add -A`: con varios cambios vivos el árbol casi nunca contiene sólo lo de este paso
