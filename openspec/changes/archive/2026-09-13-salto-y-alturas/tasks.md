## 1. El formato: alturas y versión 2

- [x] 1.1 `apps/web/src/game/level.ts`: `LevelConfig` gana `heights: number[][]`, con su JSDoc —columnas apoyadas en el suelo, hueco a 0—. Verificación: `npx tsc --noEmit` señala todos los sitios que construyen un `LevelConfig`, y se arreglan uno a uno.
- [x] 1.2 `apps/web/src/game/program.ts`: `PROGRAM_FORMAT_VERSION` a `grid-blockly-2`, con el comentario del token al día. Verificación: `grep -rn "grid-blockly-1" apps/web/src` sólo devuelve tests que prueban el rechazo. **Y un test viejo lo cazó al revés**: `program.test.ts` usaba `grid-blockly-2` como ejemplo de versión desconocida y cayó en cuanto pasó a ser la buena; ahora prueba el rechazo de la 1 y de una inventada.
- [x] 1.3 `apps/web/src/game/levelConfig.ts`: lee `heights` —misma forma que `tiles`, enteros, suelo y muro ≥ 1, hueco exactamente 0— y rechaza entero lo que no case. Verificación: tests en `levelConfig.test.ts` para una altura en un hueco, una casilla que existe con 0, una fila de alturas más corta, un decimal y un nivel en `grid-blockly-1`.
- [x] 1.4 `apps/web/src/game/debugLevel.ts`: la rejilla de pega gana alturas, **todas a 1**, para que el laboratorio y sus números medidos no cambien. Verificación: el laboratorio se ve igual que antes.

## 2. El movimiento con alturas

- [x] 2.1 `apps/web/src/game/movement.ts`: `advance` choca contra una casilla más alta (`Blocker` gana `'high'`) y baja a cualquier altura menor; nace `jumpAdvance`, que sube un nivel, pasa a la misma altura o baja, y se queda con dos o más niveles, muro, hueco o borde. Verificación: tests en `movement.test.ts` para cada fila de la tabla de `design.md`, y los doce de hoy siguen en verde.

## 3. El bloque saltar

- [x] 3.1 `apps/web/src/game/blockTypes.ts`: `JUMP_BLOCK` y el nombre de la entrada del cuerpo. Verificación: el intérprete los importa sin tocar Blockly.
- [x] 3.2 `apps/web/src/game/blocks.ts`: define `codeplay_jump` con una entrada de sentencias que sólo acepta avanzar y los giros, color propio del tema y texto «saltar»; lo añade a `FLYOUT_BLOCKS`. Verificación: `blocks.test.ts` hace la ida y vuelta de un saltar con girar y avanzar dentro, y **se imprime la salida real** para copiarla al contrato. **Hecho así:** color `mint`; que no quepa un saltar dentro de otro **no se pudo decir con los `check` de las conexiones** —un saltar colgado del `next` de un avanzar que ya está dentro conecta con el avanzar, y esa conexión es legal fuera—, así que va con una extensión que lo desengancha al soltarlo. La salida real se imprimió, se copió al contrato §4.3 y quedó fijada en el test con `toMatchObject`.
- [x] 3.3 La caja enseña los cuatro bloques sin desplazar nada, en la rejilla que ya tiene. Verificación: en el laboratorio se ven los cuatro y se arrastra saltar al lienzo y un avanzar dentro de él. **Medido** que la caja pinta los cuatro sin barra; **el arrastre y el recorrido los hizo el usuario** jugando el tablero del mundo 2 en el laboratorio, porque el panel del navegador de la sesión se quedó oculto —0 frames, `visibilityState: hidden`— y ahí no se puede verificar nada animado.

## 4. El intérprete

- [x] 4.1 `apps/web/src/game/interpreter.ts`: `Order` gana `jump` con `body`; `readProgram` lee `inputs.<cuerpo>.block` siguiendo su cadena, aguanta el `null` de un arrastre a mitad y rechaza entero un saltar dentro de otro. Verificación: tests con el JSON real de la tarea 3.2 pegado tal cual, con el `null` literal dentro del cuerpo, y con un salto anidado. **La lectura del JSON real** vive en `blocks.test.ts`, que construye el saltar en un espacio de Blockly y se lo da al intérprete; los casos que el editor no produce —null, anidado, bloque raro— van en `interpreter.test.ts` con la misma forma.
- [x] 4.2 `countSteps`: saltar vacío 1, saltar con cuerpo el doble de su cuerpo. Verificación: tests para vacío (1), con avanzar 2 (4, igual que dos con avanzar 1) y con girar y avanzar 1 (4).
- [x] 4.3 `runProgram`: un saltar vacío deja una entrada y cada paso de su cuerpo dos —despegue y aterrizaje—, con `motion` en cada `RunStep`; `stepsTaken` y la meta siguen igual. Verificación: el test que ata `countSteps` a `steps.length` se extiende a programas con saltos, y **se comprueba rompiéndolo**: dejar una sola entrada por paso saltado lo pone en rojo. **Medido:** quitando el despegue caen 3 tests —la escalera, el salto fallido y la atadura del recuento con saltos—, y el archivo se restauró byte a byte. Suite entera tras el cambio: 226 tests en verde, `tsc` y lint limpios.

## 5. La escena

- [x] 5.1 `apps/web/src/game/GameScene.tsx`: una columna de cubos por casilla según su altura, con la de arriba del color de salida, meta o damero; con todo a 1 se ve igual que hoy. Verificación: en el laboratorio, la rejilla de pega sin cambios y un tablero de prueba con columnas de 1, 2 y 3. **Medido en la escena** con el tablero del mundo 2: 12 columnas y 25 cubos, uno por nivel de cada casilla del mapa, y el personaje a altura 1 en la salida.
- [x] 5.2 El personaje encima de su columna, y el salto como arco sobre el mismo reloj de `useFrame`: en el sitio, subiendo, a la misma altura y bajando; el topetazo contra una columna más alta. Verificación: el personaje leído de la escena —casilla y altura— coincide con lo que calcula el intérprete en cada entrada de un recorrido con subida, bajada y salto en el sitio. **Lo que se verificó es menos que eso, y se dice**: el usuario jugó el nivel y dio el salto por bueno —«buenísima la animación de salto»—; **la comparación entrada a entrada contra el intérprete NO se hizo**, porque el panel del navegador de la sesión estaba oculto. La lógica que decide dónde cae la cubren los tests del intérprete.
- [x] 5.3 El encuadre con alturas: el tablero entero por encima de la bandeja en la vista de partida, **y la vista de hoy intacta** con alturas a 1. Verificación: medido con proyección de las esquinas de las columnas más altas contra el borde de la bandeja, y los tres niveles del mundo 1 con el mismo encuadre que antes. **Medido en la pantalla del nivel 2 del mundo 2** a 1440 × 900, proyectando las ocho esquinas de los 25 cubos con la cámara orientada al centro —como la dejan los controles—: el tablero cae entre y 272 y y 553, con 71 px de aire bajo el borde de arriba del juego y **73 px por encima de la bandeja**, que empieza en 627. `BOARD_LIFT` no hizo falta tocarlo. Con alturas a 1 nada del encuadre cambió, porque la cara de arriba de una columna de un cubo sigue en `y = 0`.

## 6. Los niveles del mundo 1 a la versión 2

- [x] 6.1 Escribir `supabase/migrations/202606030027_world_1_levels_format_2.sql`: los tres niveles del mundo 1, localizados por `(world_id, sort_order)`, pasan a `grid-blockly-2` en `programming_language` y en el sobre de `starter_code`, y su `validation_rules` gana `heights` —1 en cada casilla que existe, 0 en cada hueco— **sin tocar nada más** del tablero. Verificación: sin `insert`, tres `update` con `where`, y `optimalSteps` 4, 12 y 20 intactos.
- [x] 6.2 `levelConfig.test.ts` y `levelSolutions.test.ts` leen los tres tableros **de la 0027**, y la búsqueda del mínimo usa las reglas con alturas. Verificación: pasan, y siguen mordiendo con `optimalSteps` uno por debajo y uno por encima. **Medido:** leen de la 0027 y pasan; la búsqueda pasó a Dijkstra con salto a coste 2, con un tablero de escalera propio que la obliga a saltar; y con el nivel 2 a 11 y a 13 en la 0027 caen 3 tests en cada caso, con el archivo restaurado byte a byte.
- [x] 6.3 Medir las tres filas antes del `push`, contarle la 0027 al usuario en palabras y **que él lance `npx supabase db push`**. Verificación: el usuario lo dice. **Medido antes**: las tres en `grid-blockly-1` —la causa de que el mundo 1 dejara de jugarse en cuanto entró el código—, y pendientes sólo la 0027 y la 0028. El usuario lanzó el `push` de las dos juntas.
- [x] 6.4 Releer las tres filas y jugar los tres niveles en su pantalla. Verificación: se ven como antes y dan «¡Perfecto!» con 4, 12 y 20 pasos. **Hecho a medias, y se dice**: las tres filas releídas están en `grid-blockly-2` con alturas a 1 y los mismos 4, 12 y 20, `updated_at` `2026-09-13T23:59:10`; y las tres pantallas **cargan su tablero** —5, 10 y 14 columnas, sin el rechazo—, que es lo que el usuario había visto roto. **El recorrido animado NO se jugó**: el panel del navegador de la sesión estaba oculto, con 0 frames. Lo que dice que dan «¡Perfecto!» es `levelSolutions.test.ts` sobre los tableros de la 0027, no la pantalla.

## 7. El contrato y la documentación

- [x] 7.1 `docs/CONTRATO-DE-INTEGRACION.md`: §3 con el salto en la definición de paso; §4.2 sin «Sin alturas» y con `heights`, sus reglas y el ejemplo; §4.3 con `grid-blockly-2` y **el anidamiento registrado con la salida real**; §4.4 con las filas del salto y un ejemplo resuelto con subida; §8 sin la entrada del bloque con cuerpo. Sin nombres de código ni referencias a otros documentos: es el único que se escribe para quien no conoce el repositorio. Verificación: `grep -n "Sin alturas\|nunca un escalón\|grid-blockly-1" docs/CONTRATO-DE-INTEGRACION.md` sólo devuelve frases que cuentan el cambio.
- [x] 7.2 `docs/DISENO-DEL-JUEGO.md`, `docs/CONTEXT.md` §1.4 y §2.9, `docs/ROADMAP-JUEGO.md`, `supabase/README.md` y `openspec/config.yaml`. Verificación: releer cada frase que hable de alturas, versión o bloques contra lo que hoy es verdad, y `npx openspec doctor` sin quejas. **Hecho así:** `DISENO-DEL-JUEGO.md` no decía nada de alturas —`grep` sin coincidencias que tocar—; en `CONTEXT.md` §1.3, §2.7, §2.9 y §4.2b, y en §1.4 no había frase afectada; el roadmap anota que «Sin alturas» fue verdad hasta el 13-sep-2026 en vez de borrarlo; y cinco comentarios de código que contaban «tres bloques» se reescribieron. `doctor`: raíz `ok`.
- [x] 7.3 `npm run lint`, `npm run test:run` y `npm run build`, con la salida cruda y el tamaño de los trozos contra los de hoy (635,48 / 648,51 / 849,42 kB). Verificación: lint sin avisos, ningún test roto y el crecimiento explicado. **Medido:** lint sin avisos; `Test Files 21 passed (21)`, `Tests 229 passed (229)`; `✓ built in 11.37s` con **635,84 / 649,17 / 850,53 kB** —+0,36 en el principal por la comprobación de alturas de `levelConfig.ts`, que corre por encima de la frontera; +0,66 en el editor por el bloque «saltar» y su extensión; +1,11 en la escena por el salto del intérprete y las columnas—.
- [x] 7.4 Commit enumerando las rutas, con autorización del usuario. Verificación: `git status` enseña sólo lo de este cambio. **Autorizado el 13-sep-2026** en un solo commit con `nivel-2-mundo-2`, que comparte con éste la documentación y los tests de niveles, con los dos archivados dentro.
