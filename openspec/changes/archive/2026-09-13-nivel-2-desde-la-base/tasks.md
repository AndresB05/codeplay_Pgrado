## 1. La migración, escrita y sin aplicar

- [x] 1.1 Escribir `supabase/migrations/202606030025_seed_level_2_world_1.sql`: un `update` de la fila localizada **por `(world_id, sort_order = 2)`** de `selva-algoritmica`, con slug `camino-con-curvas`, título `Nivel 2 - Camino con curvas`, la descripción y la narrativa de `design.md`, `programming_language` a `grid-blockly-1`, `starter_code` con el sobre vacío y `validation_rules` con el tablero 5 × 5 de `design.md` —matriz alineada, huecos escritos `'gap'`, salida `{3,0}` al sur, meta `{0,4}`, `optimalSteps` 12—. Comentario con el boceto, la solución y por qué no hay una más corta, como la 0024. Verificación: sin `insert`, sin `update` sin `where`, y la matriz tiene 5 filas de 5.

## 2. Los tests del puzle sembrado

- [x] 2.1 En `apps/web/src/game/levelConfig.test.ts`, aceptar el `config` del nivel 2 **leído de la 0025 con `?raw`**, y reescribir el comentario de `previousGame`: son las reglas que la 0012 sembró en el nivel 2, que ya no son las suyas. Verificación: `npm run test:run` pasa, y cambiar a mano el `optimalSteps` del `.sql` lo pone en rojo —se deshace después—.
- [x] 2.2 Crear `apps/web/src/game/levelSolutions.test.ts`: por cada nivel sembrado —el 1 desde la 0024 y el 2 desde la 0025—, (a) la solución a mano ejecutada con `runProgram` llega a la meta y `countSteps` da `optimalSteps`; (b) una búsqueda en anchura sobre (casilla, orientación), con avanzar una casilla y girar a coste 1, da exactamente `optimalSteps` como mínimo. Verificación: pasa, y **muerde en las dos direcciones**: con `optimalSteps` 11 caen (a) y (b), y con 13 también —las dos comparan igualdad, así que cada una caza las dos direcciones por su lado—; comprobado cambiándolo en el `.sql` y deshecho, con el archivo verificado byte a byte después.

## 3. Enseñar el tablero antes de sembrar

- [x] 3.1 **Temporal.** En `apps/web/src/components/dashboard/student/StudentGameLabModule.tsx`, montar el `config` leído de la 0025 en lugar de `debugLevel`. Verificación: el laboratorio dibuja un 5 × 5 con 10 cubos en zigzag y ninguno en las otras 15 casillas.
- [x] 3.2 Jugar la solución de `design.md` en el laboratorio, con las trampas del `CONTEXT.md` §4.10 delante: `resize_window` con tamaño concreto, frames comprobados antes de fiarse de nada, `.click()` desde `javascript_tool`. Verificación: **la posición del personaje leída del grafo** —`scene.getObjectByName('character')`— acaba en la meta, y el contador y la barra dicen 12 pasos y «¡Perfecto!».
- [x] 3.3 Capturar las **cuatro vistas** de `design.md` —la de hoy, isométrica, frontal y alta— como PNG a disco, **verificar los archivos**, y mandárselas al usuario con `SendUserFile`. Verificación: el usuario elige una. **Eligió la 1, la de hoy**, el 13-sep-2026.
- [x] 3.4 **No aplica: el usuario eligió la vista de hoy**, así que `CAMERA_START` y `BOARD_LIFT` no se tocan y el nivel 1 no cambia. Lo que habría hecho, **sólo si elige una vista distinta de la de hoy**: cambiar `CAMERA_START` en `apps/web/src/game/GameScene.tsx`, volver a medir `BOARD_LIFT` para que el tablero no toque la bandeja, y enseñarle **también el nivel 1** con la vista nueva antes de dejarla. Verificación: el usuario da por buenos los dos niveles con esa vista.
- [x] 3.5 Enseñarle el texto de la tarjeta y de «Instrucciones», y decirle que el truco va en la narrativa y por qué. Verificación: el usuario da el texto por bueno o lo corrige, y la migración recoge lo que diga.
- [x] 3.6 Revertir el laboratorio a `debugLevel`. Verificación: `git diff apps/web/src/components/dashboard/student/StudentGameLabModule.tsx` sale vacío.

## 4. La parada del SQL y la siembra

- [x] 4.1 Medir la fila del nivel 2 en la base **antes** del `push`: slug, título, `programming_language` y `validation_rules`. Verificación: los valores medidos se anotan, y deben ser los de la 0012. **Medido el 13-sep-2026**, con el cliente de la aplicación y la sesión del niño: **una sola fila** casa con `(selva-algoritmica, sort_order 2)`, id `53dc264e-e290-4454-93cb-85555f3e27d9`, slug `puente-condicional`, título «Puente Condicional», `programming_language` `javascript`, `validation_rules` `{"goal":"choose_safe_path","requiresCondition":true}`, `xp_reward` 100 y `updated_at` `2026-09-11T18:36:58.232779+00:00`. Y `npx supabase migration list` da de la 0001 a la 0024 aplicadas y **sólo la 0025 pendiente**.
- [x] 4.2 **Parar.** Contarle al usuario en palabras qué fila toca la 0025 y con qué valores, y esperar a que la dé por buena. **El `npx supabase db push` lo lanza él.** Verificación: el usuario lo dice.
- [x] 4.3 Volver a leer la fila después del `push` y compararla con lo medido en 4.1. Verificación: los cuatro campos han cambiado a los de la 0025 —un `where` que no case actualiza cero filas y no falla—. **Medido el 13-sep-2026, tras el `push` del usuario:** la misma fila `53dc264e…` pasa a slug `camino-con-curvas`, título, descripción y narrativa nuevos, `grid-blockly-1`, sobre vacío y el tablero 5 × 5 con `optimalSteps` 12; `updated_at` avanza de `2026-09-11T18:36:58` a `2026-09-13T22:08:35`, y las filas de los niveles 1 y 3 conservan su `updated_at`. `migration list` da la 0025 aplicada.
- [x] 4.4 Jugar el nivel 2 **en su pantalla de nivel**, entrando desde la lista del mundo. Verificación: el personaje leído del grafo llega a la meta y la barra dice «¡Perfecto! Llegaste a la meta con 12 pasos, justo lo que cuesta la mejor solución». **Hecho el 13-sep-2026**: la tarjeta de la lista dice «Nivel 2 - Camino con curvas» con su descripción, la pantalla enseña la narrativa con el truco, el tablero tiene 10 cubos y el personaje sale en fila 3 columna 0 mirando al sur. Con 73 frames y `hidden: false`, el editor de **esa** pantalla publicó —desapareció el texto del lienzo vacío y la barra no dijo «No hay bloques que ejecutar»— y el recorrido pisó las diez casillas en orden hasta la meta, con «Pasos: 12» y la frase de arriba literal.

## 5. Documentación y cierre

- [x] 5.1 `supabase/README.md`: las entradas de la **0024**, que falta, y de la 0025, y «veintitrés migraciones» a veinticinco. Verificación: la lista llega a la 25 sin huecos entre la 21 y la 25.
- [x] 5.2 `docs/CONTEXT.md`: §2.7 con la 0025 en la tabla y el recuento de migraciones; §2.9 con `levelSolutions.test.ts`; §4.2b con el nivel 2 rediseñado y **siete** pendientes, releyendo cada frase contra lo que hoy es verdad. Verificación: `grep -n "Quedan ocho\|los otros ocho" docs/CONTEXT.md` no devuelve nada que siga siendo falso. **Y de paso, encontrado con ese mismo `grep` ampliado:** el recuento de §2.7 decía «las siete restantes» con 24 migraciones —caducado desde el J7.1—; §1.3 decía 24; y cuatro frases contaban filas que ya no cuadran —el comentario de `StudentLevelModule.tsx` («las ocho filas»), los de `levelConfig.test.ts` y `program.test.ts` («las nueve filas») y dos del apéndice de `docs/CONTRATO-DE-INTEGRACION.md`—, reescritas sin número. **No se tocaron**: `docs/CONTEXT.md` §2.9 en la nota del J3, que cuenta lo que era verdad entonces, y `docs/ESTADO-DEL-PROYECTO.md`, que dice 22 migraciones desde antes del J7.1 y cuyas discrepancias recoge `CONTEXT.md` §5.
- [x] 5.3 `docs/ROADMAP-JUEGO.md` §3: J7.2 hecho, con lo que acabó siendo. Y si la vista cambió, anotarlo donde el J7.1 dejó escrita la suya. Verificación: la fila del J7.2 dice ✅.
- [x] 5.4 `openspec/config.yaml`: el recuento de migraciones, que hoy dice 22. Verificación: `npx openspec doctor` no se queja. **Además** la prioridad 4 dice ya que se juegan los niveles 1 y 2 y que la lista no bloquea hasta la prueba preliminar.
- [x] 5.5 Ejecutar `npm run lint`, `npm run test:run` y `npm run build` y pegar la salida cruda. Verificación: lint en cero avisos, ningún test roto y el build termina.
- [x] 5.6 Preparar el commit enumerando las rutas en `git add`, nunca `-A`, y pedir al usuario que lo autorice. Verificación: `git status` enseña que lo que entra es sólo lo de este cambio, sin las capturas. **El usuario pidió un solo commit para este cambio y `niveles-sin-candado`**, que comparten `CONTEXT.md`, `config.yaml` y `StudentLevelModule.tsx`, con los dos ya archivados dentro. Las capturas se borraron antes y nunca estuvieron en el árbol.
