## 1. La línea de partida

- [x] 1.1 `npm run build` **antes de tocar nada** y comprobar que salen las cifras de `e3daa4d`: principal **625,00 kB** (167,90 gzip), `program` **0,35** (0,24), `BlockEditor` **644,43** (172,75), `GameScene` **830,27** (224,28), **221 módulos**. Si no cuadran, parar: la comparación del punto 5 no valdría
- [x] 1.2 `npm run test:run` y comprobar **156 tests en 19 archivos**. Si falla alguno de ResetPassword, ChangeNamePanel, Signup, TeacherPanelModule o ClassroomsProvider, repetir con la máquina libre antes de tocar nada: los cinco llevan `waitFor` de varios segundos y fallan de forma intermitente con la máquina cargada, y es **preexistente**

## 2. `interpreter.ts` — el coste del lienzo, en una función que se pueda probar

- [x] 2.1 Escribir `programCost(program: Program | null): { steps: number; rootCount: number } | null` en `apps/web/src/game/interpreter.ts`: abre el sobre con `openProgram`, lee con `readProgram` y cuenta con `countSteps`. Devuelve `null` cuando no hay programa, cuando no se puede leer y cuando **no hay ninguna orden**. Verificar leyendo los `import` que el archivo sigue **sin Blockly, sin `three` y sin JSX**, y con `npx tsc --noEmit -p apps/web` que compila
- [x] 2.2 Dejar en su encabezado **por qué colapsa los tres casos en `null` pudiendo distinguirlos**, que es lo que la separa de `start()`: la barra del final tiene que distinguirlos porque cada uno lleva su texto, y este indicador no debe porque en los tres no pinta nada (design.md §2). Comentario de *por qué*, no de *qué* (CLAUDE.md)
- [x] 2.3 Añadir a `interpreter.test.ts` el bloque de `programCost`: `null` con `null`, `null` con una versión de formato desconocida, `null` con el lienzo vacío `{}`, **10 pasos y `rootCount` 1** con el PROGRAMA A del contrato §4.3 que ya está pegado en el archivo, y **`rootCount` 2** con los dos montones sueltos que también están. Verificar con `npm run test:run`
- [x] 2.4 Añadir el test de que **las dos cadenas dan lo mismo**: sobre un mismo `WorkspaceState`, `programCost(sealProgram(ws))?.steps` y `countSteps(readProgram(ws)!.orders)`. Hoy **no puede fallar** —son la misma composición—, y por eso está: `start()` y `programCost` hacen esa composición por caminos separados, y el día que alguien toque una y no la otra, el número que se enseña al construir y el que se enseña al terminar dejan de coincidir. Es el mismo género que el test de igualdad del J6, y va con el comentario que dice para qué está
- [x] 2.5 Comprobar que **no se ha tocado** `readProgram`, `countSteps` ni `runProgram` —`git diff` de este archivo sólo añade—: este paso no cambia lo que se ejecuta ni cómo se cuenta

## 3. `GameScene.tsx` — las dos piezas, montadas independientes

- [x] 3.1 Añadir el coste del lienzo con `useMemo(() => programCost(program), [program])`, leyendo **la prop y no el `useRef`**. Verificar que el `useRef` de `latest` y `start()` **no se tocan**: ese `useRef` existe para que la ejecución no dependa de la prop, y esa decisión del J5 sigue en pie (design.md §1)
- [x] 3.2 Pintar la línea del coste cuando `programCost` no dé `null` **y no haya ejecución en curso**: «Tu programa cuesta N pasos. La mejor solución cuesta M pasos.», con el `stepsLabel` que ya existe para el singular. Verificar en el navegador (punto 4) que con el lienzo vacío y con el programa ilegible **no aparece ninguna línea**
- [x] 3.3 Añadir el aviso de sueltos **en presente** cuando `rootCount > 1` al construir —«Tienes bloques sueltos: sólo se ejecutará el montón de más arriba.»—, con **texto propio** y sin reutilizar `LOOSE_BLOCKS_WARNING`, que habla en pasado de una ejecución que aquí no ha ocurrido (design.md §7). Verificar que el del J6 sigue saliendo igual al terminar
- [x] 3.4 Cambiar el texto de «Ejecutando el programa…» por «Ejecutando el programa… paso K de N», con **K = `index + 1`** y **N = `attempt.steps`**, el recuento leído. Verificar que N **no** sale de la prop `program` ni de `programCost`: si saliera de ahí, mover un bloque a mitad de recorrido cambiaría el contador que el niño está viendo correr (design.md §1 y §8)
- [x] 3.5 Comprobar la separabilidad **borrando de verdad**: quitar el `useMemo`, la línea del coste, sus dos textos y el `import` de `programCost`, y verificar que la escena compila, que el contador de la ejecución **sigue funcionando igual** y que el resultado del J6 no cambia. Deshacer después. Si hace falta tocar algo más para que compile, están enredadas y hay que rehacer el montaje (design.md §4)
- [x] 3.6 Verificar que `StudentGameLabModule.tsx` **no ha hecho falta tocarlo** y que no hay hexadecimales sueltos en lo añadido: los colores salen de los nombres del tema (CLAUDE.md)

## 4. La verificación en el navegador

- [x] 4.1 `npm run dev`, abrir `/dashboard/game` y **emular un viewport con `resize_window`** antes de mirar nada animado. **Contar frames con `requestAnimationFrame` NO sirve**: con el panel oculto la página da 60 fps y `document.hidden` sigue en `false` con la escena congelada igualmente —lo que se suspende es el bucle de `@react-three/fiber`—, y un recorrido de 3,4 s se quedó clavado 8,6 s (`CONTEXT.md` §2.9). Los clics del panel no disparan los botones: usar `.click()` desde `javascript_tool`
- [x] 4.2 Cargar programas de un golpe importando desde la página `blockly_core.js?v=…`, el mismo módulo que ya importó la aplicación, y usar `getMainWorkspace()` y `serialization.workspaces.load`: dispara el escuchador del editor y no hace falta arrastrar nada (`CONTEXT.md` §2.9)
- [x] 4.3 **El coste sube y baja al construir**: cargar el PROGRAMA A y verificar que dice **10 pasos contra los 10 de la mejor solución** sin haber ejecutado nada; encadenar un bloque más y verificar que el número sube; quitarlo y verificar que vuelve a bajar
- [x] 4.4 **Los montones sueltos al construir**: con dos montones en el lienzo, verificar que el coste que se enseña es el del montón de más arriba —no la suma— y que sale el aviso en presente. **Y arrastrar un bloque fuera de la cadena y volver a encajarlo**, para ver si el aviso parpadea: es el riesgo de design.md §7. **Anotar lo que se vea**; si molesta, se revisa con el caso delante en vez de decidirlo a ciegas
- [x] 4.5 **Lienzo vacío y programa ilegible**: verificar que **no** aparece ninguna línea de coste —ni «0 pasos»— y que la consola no saca ningún error. **El programa ilegible NO se pudo provocar desde el editor**, y queda sin observar por separado: el editor sólo produce los tres bloques conocidos y su campo de pasos es numérico y acotado, así que `readProgram` no tiene por dónde devolver `null`. Ese camino lo cubre el test de `programCost`. Es el mismo género de hueco que el J6 dejó anotado con «antes de que el editor publique»
- [x] 4.6 **El contador durante la ejecución**: ejecutar el PROGRAMA A y verificar que se ve «paso K de 10» subiendo con el personaje, que **la línea del coste del lienzo desaparece** mientras corre, y que al terminar el contador deja sitio al resultado con el mismo número —«paso 10 de 10» y después «Usaste 10 pasos»—
- [x] 4.7 **El contador NO se mueve al tocar el lienzo con el recorrido en marcha**, que es la garantía que este paso no puede romper: lanzar un programa largo y cargar otro distinto a media ejecución con `serialization.workspaces.load`. El contador tiene que seguir diciendo el total del que se está ejecutando, y el personaje terminar el recorrido que empezó
- [x] 4.8 **«Reiniciar» a mitad**: verificar que el contador desaparece y que el coste del lienzo vuelve a verse
- [x] 4.9 **Después de una ejecución**: verificar que el resultado del J6 y la línea del coste conviven, y que al tocar un bloque la línea pasa a decir lo que cuesta lo nuevo mientras el resultado sigue hablando en pasado del recorrido que hubo (design.md §6)
- [x] 4.10 **Mirar con las dos líneas delante si la barra se repite**: al terminar el PROGRAMA A sin tocar nada dice el resultado **y** el coste del lienzo, y «la mejor solución» aparece dos veces. Si sobra, la salida que **no acopla nada** es que la línea del coste **deje de decir el óptimo** —«Tu programa cuesta 10 pasos» y punto—, porque ninguna pieza tiene que mirar a la otra para eso. **Visto y decidido: se queda como está.** La salida propuesta choca con lo que el usuario pidió —«lo que cuesta el programa del lienzo, **contra el óptimo del nivel**»—: quitarle el óptimo a esa línea deja al niño **sin nada contra lo que comparar mientras construye**, que es justo la lección del paso, y el resultado no puede suplirlo porque antes de la primera ejecución no hay resultado. La repetición se limita al estado «recién terminado y sin tocar nada», y se deshace en cuanto mueve un bloque. Anotado en `design.md` §9

- [x] 4.11 **El cruce de los dos avisos**, que se vio revisando el código y no diseñándolo: ejecutar con montones sueltos y mirar la barra AL TERMINAR. Salían cuatro líneas, y las dos últimas decían lo mismo en presente y en pasado. **Corregido**: el aviso del lienzo cede cuando el del resultado está en pantalla. Verificado que al construir sale el del lienzo, al terminar sólo el del resultado, y que «Reiniciar» devuelve el del lienzo
- [x] 4.12 **Comprobar que el programa LLEGA a la aplicación y no sólo al SVG del editor**, en pestaña recargada de cero: el `<pre>` del sobre tiene que contener `codeplay_advance` después de cargar. Sin esa comprobación, un lienzo que se pinta y no publica deja por verificado lo que no lo está

## 5. Las medidas del bundle

- [x] 5.1 `npm run build` y comparar contra 1.1. El principal **no debe subir** de 625,00 kB y lo que crezca tiene que salir en el trozo de `GameScene`. Anotar las cinco cifras y los módulos. **Medido**: principal **625,00 kB** (167,90) sin moverse, `program` **0,35**, `BlockEditor` **644,43**, `GameScene` **830,27 → 830,87** (224,28 → 224,43), **221 módulos** iguales
- [x] 5.2 Verificar que la importación nueva `interpreter.ts` → `program.ts` **no ha movido nada al trozo principal**: contar con `grep -o … | wc -l` —nunca `grep -c`, que en un archivo minificado da 1— y comprobar que `rootCount` y los textos de la barra siguen dando **cero** en el principal. **`programCost` no sirve de marca**, por lo mismo que `countSteps` en el J6: el minificador renombra los nombres locales y un cero ahí no prueba nada — **comprobado: da 0 en los dos**. **Medido** con las marcas que sí sirven: `rootCount` 0 en el principal y 7 en `GameScene`; «Tu programa cuesta», «Tienes bloques sueltos» y «Ejecutando el programa», 0 en el principal y 1 en `GameScene`

## 6. El contrato y la documentación

- [x] 6.1 `docs/CONTRATO-DE-INTEGRACION.md` §4.2: corregir la frase «el niño lo ve en pantalla **al terminar**», que deja de ser cierta —`optimalSteps` se ve ahora también mientras construye—. **El argumento no cambia y es el que autoriza esto**: un número no es una solución
- [x] 6.2 `docs/ROADMAP-JUEGO.md` §3: el J6.1 pasa a ✅
- [x] 6.3 `docs/CONTEXT.md` §2.9: las dos piezas, **las dos fuentes que no se mezclan** y por qué —con el `useRef` del J5 delante—, `programCost` y sus tests, qué se borraría para retirar la pieza de construcción, y qué quedó verificado en el navegador. Anotar también lo que se vio en 4.4
- [x] 6.4 `docs/CONTEXT.md` §4.8: las medidas del punto 5, con el título del apartado revisado si alguna cifra cambia de kilobyte redondo
- [x] 6.5 `openspec/config.yaml`: repasar si el bloque de stack o el de convenciones cambian —no entra ninguna dependencia, así que probablemente no— y verificar con `npx openspec doctor` que el YAML sigue parseando. **No cambia**: no entra dependencia, no hay módulo nuevo y `interpreter.ts` ya figura en su lista de módulos puros. `doctor` en verde

## 7. Verificación final

- [x] 7.1 `npm run lint` con **0 warnings**, `npm run test:run` con **163 en 19 archivos** —los 156 de antes más los 7 de `programCost`: los cuatro casos que muerden, el sobre con versión desconocida, el programa ilegible y el de las dos cadenas— y `npm run build` sin error, los tres **desde la raíz**: `npx vitest run` se salta la configuración del workspace y da decenas de fallos falsos
- [x] 7.2 **PARADA antes de commitear.** No se commitea hasta que la sesión que revisa lo haya verificado contra el disco
- [x] 7.3 Enumerar las rutas en `git add` una a una, **nunca `git add -A`**: con varios cambios vivos el árbol casi nunca contiene sólo lo que se está commiteando (CLAUDE.md, commit `982a299`)
- [x] 7.4 **PARADA antes del commit del archivado.** El `## Purpose` de `openspec/specs/juego-3d/spec.md` cuenta **trece** garantías y dice que el recuento se ve al terminar; pasan a **quince** y **lo nuevo se añade al final, sin renumerar las referencias internas**. Ningún delta transporta el Purpose: se reescribe a mano
- [x] 7.5 En esa misma parada, leer el **diff del requisito modificado** —«Al terminar se ve lo que costó y lo que costaba lo bueno»— y confirmar que lo que cambió son **dos sitios y ni una línea más**: la frase de «mientras la ejecución está en curso» y el escenario «La ejecución está en curso». Es el punto 8 de `ROADMAP.md` §1.3 aplicado a un `MODIFIED`, que pierde detalle igual que un `REMOVED` mal copiado y sin que nada lo delate
