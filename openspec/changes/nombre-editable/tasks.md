## 1. La regla de longitud, declarada una vez

- [x] 1.1 Crear `apps/web/src/components/auth/fullName.schema.ts` con la regla del nombre: `z.string().trim()`, `min(2, …)` reutilizando **el mismo texto** que hoy tiene `SignupForm.schema.ts:7` —«El nombre debe tener al menos 2 caracteres»— y `max(60, …)` con su mensaje nuevo. El **60 está decidido en `design.md` §4 y no se decide al implementar**. Un comentario corto explica por qué la regla vive aquí y no en el esquema del registro, y que es la **única** validación del nombre porque la RPC no tiene ninguna. Verificar que el archivo exporta un solo símbolo y que `npm run build` compila.
- [x] 1.2 En `apps/web/src/components/auth/SignupForm.schema.ts`, sustituir el `fullName: z.string().min(2, …)` de la línea 7 por la regla importada. **No tocar nada más del esquema**, en particular el `min(6)` de la contraseña, que sigue duplicado en `ChangePasswordForm.schema.ts` y es otro cabo. Verificar con `grep -n "min(2"` que ya no queda ese literal en `SignupForm.schema.ts` y que `npm run test:run` sigue pasando `Signup.test.tsx`.
- [x] 1.3 En `apps/web/src/pages/Signup/Signup.tsx:87`, enviar `parsedForm.data.fullName` en lugar del estado en crudo. **Es este cambio quien lo obliga**: hoy los dos valores son idénticos y la divergencia la abre el `trim()` de la 1.1, que dejaría el registro validando recortado y guardando sin recortar. No se toca ningún otro argumento de la llamada a `signUp`. Verificar que `npm run test:run` sigue pasando `Signup.test.tsx`.
- [x] 1.4 Comprobar **en pantalla** que el registro sigue funcionando y que ahora rechaza un nombre largo: con `npm run dev`, en `/signup`, un nombre de más de 60 caracteres no envía nada y muestra el máximo. Anotar aquí el texto exacto que salió. Este es el escenario «El registro exige la misma longitud» del delta. **Resultado:** con 61 caracteres el registro no envía nada y muestra «El nombre no puede pasar de 60 caracteres».

## 2. La acción en el contexto de autenticación

- [x] 2.1 En `apps/web/src/context/AuthContext.ts`, añadir a `AuthContextValue` la acción `updateFullName: (fullName: string) => Promise<boolean>`, con un bloque de documentación que diga **por qué está aquí y no en `useProfile()`**: siete sitios leen `user.fullName` de este provider y `useProfile` mantiene su propia copia. Seguir la forma de los comentarios de `changePassword` y `updateRole`, que ya explican sus decisiones ahí mismo.
- [x] 2.2 En `apps/web/src/context/AuthProvider.tsx`, implementarla junto a `updateRole` (línea 315), del que se copia la forma: `setError(null)`, llamada a `profileService.updateProfile({ fullName }, session?.user.email ?? null)`, `setError` y `false` si hay error, `setUser(result.data)` y `true` si no. **El correo se pasa aparte y no es opcional pasarlo**: `mapProfileRowToUser` lo recibe como segundo argumento porque no está en `profiles`, y sin él guardar el nombre borraría el correo de la pantalla de cuenta. Comprobar además que `!result.data` devuelve `false` en vez de escribir un vacío, como hace `updateRole`.
- [x] 2.3 **No tocar `loading`.** Verificar leyendo el archivo que la acción nueva no lo enciende, por lo mismo que las tres de contraseña y `updateRole`: las dos guardas de ruta cambian su subárbol entero por un spinner y desmontarían el formulario a mitad de operación.
- [x] 2.4 Añadir `updateFullName` al `useMemo` del valor del contexto **y a su array de dependencias**. Sin lo segundo, el consumidor se quedaría con la primera versión de la función.
- [x] 2.5 Añadir `updateFullName: async () => false` a `apps/web/src/test/buildAuthValue.ts`, que es el doble que se rompe cada vez que `AuthContextValue` gana una acción. Verificar que `npm run build` pasa: `tsc` es quien lo señala.

## 3. El panel compartido

- [x] 3.1 Crear `apps/web/src/components/dashboard/shared/ChangeNamePanel.tsx` con la estructura de `ChangePasswordPanel.tsx`: botón que pliega y despliega, `useAuth()` como única dependencia de datos, `SignupField` para el campo, indicador de envío propio, aviso de error con `role="alert"` y confirmación con `role="status"`. Cabecera de documentación que diga por qué vive en `shared/` —lo montan las dos pantallas de Ajustes— y por qué escribe por `useAuth()`.
- [x] 3.2 El campo **arranca con el nombre actual** (`user?.fullName ?? ''`), no vacío, y al plegar y volver a desplegar vuelve a ese valor. Es la única diferencia de forma con el panel de contraseña, y está en el escenario «El formulario llega con el nombre actual».
- [x] 3.3 Validar con la regla de la tarea 1.1 **antes** de llamar a `updateFullName`, y enviar el valor **ya recortado** que devuelve el `safeParse`, no el estado en crudo. Un nombre corto, uno largo y uno hecho sólo de espacios no llegan al servidor y muestran su motivo.
- [x] 3.4 Montar el panel en `apps/web/src/components/dashboard/student/StudentSettingsModule.tsx`, junto a `ChangePasswordPanel`, dentro de la tarjeta «Detalles del explorador».
- [x] 3.5 Montar el panel en `apps/web/src/components/dashboard/teacher/TeacherSettingsModule.tsx`, junto a `ChangePasswordPanel`, dentro de la tarjeta «Detalles del usuario». Verificar que **el mismo componente** está en los dos sitios y que no se duplicó marcado: es el escenario «Las dos pantallas de Ajustes ofrecen lo mismo».
- [x] 3.6 Verificar con `grep` que el panel **no toca `username`, `avatarKey` ni `countryCode`**, y que `updateFullName` sólo pasa `fullName` al servicio: el escenario «El resto del perfil no se toca». El `username` queda fuera a propósito —la RPC lo rechaza con un `22023` crudo y `profile.service.ts` sigue devolviendo inglés, `docs/CONTEXT.md` §4.5—.

## 4. «Sr. Robot», una sola declaración

- [x] 4.1 Exportar `FALLBACK_TEACHER_NAME = 'Sr. Robot'` desde `apps/web/src/services/classrooms.service.ts`, junto a `FALLBACK_STUDENT_NAME` (línea 64), con un comentario que diga por qué vive ahí: llega a la base por `CreateGroupForm.tsx:52`, que lo escribe en `classrooms.teacher_name` cuando el campo se deja vacío. **No cambiar el valor.**
- [x] 4.2 Retirar la constante local de `apps/web/src/pages/TeacherDashboard/TeacherDashboard.tsx:12` e importar la del servicio.
- [x] 4.3 Sustituir el literal por la constante importada en `TeacherSettingsModule.tsx:20`, `TeacherSidebar.tsx:30` y `TeacherTopBar.tsx:19`.
- [x] 4.4 Verificar con `grep -rn "Sr. Robot" apps/web/src` que queda **una sola** aparición, la del servicio. Es el escenario «Tutor sin nombre».
- [x] 4.6 **Añadida durante el apply, con el visto bueno del usuario.** Quitar el correo inventado de `TeacherSettingsModule.tsx:22` —`user?.email || 'tutor@codeplay.co'`— y dejar el marcado del caso vacío que ya usa `StudentSettingsModule.tsx:97`. Salió a la vista al montar el panel, dos líneas encima. No es alcance nuevo: **incumple un requisito que ya está en el spec principal** —«El correo NO SHALL tener ningún valor de repliegue»—, escrito por el paso 28, que lo dejó fuera por el mismo motivo que dejó «Sr. Robot»: su encargo decía «el panel del niño». Verificar con `grep -rn "@codeplay.co" apps/web/src` que no queda ninguno.
- [x] 4.5 Verificar que `SidebarPlayerCard.tsx:20` y `WelcomeBanner.tsx:26` **siguen intactos** con su literal `'Explorador'`: son huérfanos y se rehacen en su paso, no en éste.

## 5. Los dos tests

- [x] 5.1 Crear `apps/web/src/components/dashboard/shared/ChangeNamePanel.test.tsx` con el test que **fija lo que pinta la pantalla**: montar el panel dentro de un `AuthProvider` real, con `profileService.updateProfile` sustituido para devolver un perfil con el nombre nuevo y `getProfile` sembrando el viejo, y junto al panel un consumidor que pinte `user.fullName`. Guardar y comprobar que **lo pintado cambió**. Reutilizar el patrón de sonda y de mock parcial de `profile.service` que ya tiene `AuthProvider.test.tsx`. Un test que sólo compruebe que `updateProfile` fue llamado **no sirve**: pasa igual con el diseño roto, que deja la pantalla con el nombre viejo.
- [x] 5.2 En el mismo archivo, un segundo `it(` que fije que un nombre por encima del máximo **no llega al servicio**: `updateProfile` no se llama y el motivo aparece en pantalla. Es la única defensa que hay, porque la RPC no valida nada.
- [x] 5.3 Ejecutar `npm run test:run` y comparar **por el nombre de cada `it(`**, no por el total: se parte de **77 en 9 archivos** y se llega a **79 en 10**, sin ningún `it(` retirado. **Resultado:** 79 en 10, comparados contra `HEAD` uno a uno —cero retirados, dos añadidos—. Comprobado además que el 5.1 **falla** si se le quita el `setUser` a `updateFullName`: es la mitad que le da valor.

## 6. Comprobación en pantalla

- [x] 6.1 Con `npm run dev` y la **cuenta de niño** de `apps/web/.env`: cambiar el nombre desde Ajustes y comprobar **sin recargar** los cuatro sitios del panel del niño —barra lateral (`Sidebar.tsx:106`), saludo de mundos (`StudentWorldsModule.tsx:299`), pantalla de cuenta (`StudentSettingsModule.tsx:30`) y la tabla de su salón—. **Resultado:** la cuenta de niño tenía el nombre vacío y se veía «Explorador» por todas partes. Cambiado a «Ana Lucía Restrepo», los cuatro sitios pasaron a mostrarlo **sin recargar** —el saludo recorta al primer nombre, «¡HOLA, Ana!»—, y no quedó ni un «Explorador» de repliegue en pantalla.
- [x] 6.2 Con la **cuenta de tutor** de `apps/web/.env`: lo mismo en los tres sitios del panel del tutor —`TeacherSidebar.tsx:30`, `TeacherTopBar.tsx:19` y `TeacherSettingsModule.tsx:20`— más el `TeacherDashboard.tsx:32` que alimenta el formulario de crear salón. **Resultado:** «Profe Prueba» → «Camila Ortiz Bedoya» en los tres a la vez y sin recargar, cero apariciones del anterior; y el campo «Profesor a cargo» de crear salón llegó ya con el nombre nuevo por defecto. Antes de esto se comprobó el escenario del valor inicial: al abrir el formulario, el campo traía «Profe Prueba».
- [x] 6.3 Comprobar que **el correo sigue en pantalla** después de guardar el nombre, en las dos cuentas. Es la trampa de `design.md` §1: el correo viaja aparte del perfil y se pierde si no se le pasa a `mapProfileRowToUser`. **Resultado:** sigue en las dos —`user.kid2@gmail.com` y `user.tutor@gmail.com`—. En la del tutor el correo que se ve es ya el real, por la tarea 4.6.
- [x] 6.4 Comprobar que el nombre **persiste al recargar** y al volver a entrar: es lo que separa «se pintó» de «se guardó». **Resultado:** tras recargar entera la aplicación, la tabla del salón del niño trae «Ana Lucía Restrepo» —dato que viene de la vista del roster, o sea de la base, no del estado del navegador—.

## 7. Cierre

- [x] 7.1 Ejecutar `npm run lint`, `npm run test:run` y `npm run build`. Los tres pasan hoy y no deben romperse. **Resultado:** lint sin avisos, 79 tests en 10 archivos, build en 2,3 s.
- [x] 7.2 Actualizar `docs/CONTEXT.md` §2.2: añadir a la tabla de `auth-sesion` la capacidad nueva —cambiar el propio nombre desde Ajustes— con las rutas reales, y ajustar el punto del tratamiento genérico ahora que el del tutor también es único. Es una capacidad del producto, así que va a «aplicadas».
- [x] 7.3 Anotar en `docs/CONTEXT.md` §4 **un solo cabo suelto nuevo**, que se anota y no se cierra: que la longitud del nombre sólo la valida el cliente, porque `update_my_profile` no valida `full_name` en absoluto y un `check` exigiría migración. Y anotar aparte, como **decisión** y no como pendiente, que el «Profesor …» de un salón no sigue al perfil: la migración 0013 lo dejó escrito encima de la columna —`tutor_id` es la identidad, `teacher_name` es una etiqueta de texto libre—, así que un tutor renombrado no cambia el nombre de los salones que ya creó y eso es lo correcto.
- [x] 7.4 Dejar escrito, donde `docs/ROADMAP.md` §3.4 describe el punto 4 del paso 14, que **ese paso no mengua: cambia de forma.** Este cambio da control sobre **un solo** nombre, el que ven el tutor y los compañeros por igual; el paso 14 sigue debiendo el apodo y la proyección doble del roster, y ahora además parte de que **el tutor ya no tiene garantizado el nombre del registro**.
- [x] 7.5 Marcar la fila **29 ★** en `docs/ROADMAP.md` §2, colocada **después de la 28 y antes de la 16**, con su motivo en §2.1 —lo pidió el usuario, la fontanería estaba entera y sin usar desde la 0006, y no depende de ningún paso posterior—.
- [x] 7.6 Comprobar si `openspec/config.yaml` necesita réplica —sólo si cambiaron stack, estructura, convenciones o prioridades— y que `npx openspec doctor` sigue parseando. **Resultado:** se replicó el bloque de estado del paso 29 y el recuento de tests (77 → 79); `doctor` responde «OpenSpec root: ok».
- [ ] 7.7 Preparar el commit **enumerando las rutas** en `git add`, nunca `git add -A`. Con varios cambios vivos, el árbol casi nunca contiene sólo lo que se está commiteando.
