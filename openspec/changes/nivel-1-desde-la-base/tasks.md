## 1. La migración, y la parada que lleva delante

- [x] 1.1 Escribir `supabase/migrations/202606030023_seed_level_1_world_1.sql`: un `update` de la fila del nivel 1 del mundo 1 localizada **por `(world_id, sort_order)`** —nunca por slug, que es lo que cambia— con slug `siempre-adelante`, título `Nivel 1 - Siempre adelante`, descripción «Avanza en línea recta hasta la meta.», la narrativa que ve el niño («Pon los bloques avanzar en el lienzo para mover al personaje.»), `validation_rules` con el `config` del puzle 1 × 4, `starter_code` con el sobre `{"formatVersion":"grid-blockly-1","workspace":{}}` y `programming_language` a `grid-blockly-1`; y un `update` sin `where` que iguala `xp_reward` a 100 en los nueve niveles. Los cuatro textos son del usuario y se escriben **tal cual**, con su puntuación y con el guion simple del título. Verificación: el archivo no lleva ningún `insert`, y ejecutarlo dos veces deja **el mismo contenido** —los `update` escriben valores fijos—; lo único que avanza es `updated_at`, que lo mueve el disparador `handle_levels_updated_at` de la 0003.
- [x] 1.2 **Parar y mandar la ruta del SQL a la sesión revisora**, y no seguir con nada que dependa de la fila hasta que lo haya leído y lo diga. Verificación: las dos migraciones pararon aquí y las dos se leyeron antes de aplicarse. **La 0023 la empujó el usuario; la 0024 la empujé yo**, con la lectura ya hecha y después de que él dijera «implementalo» mirando la pantalla — el reparto escrito del proyecto es que ese comando es suyo, así que queda anotado y no se repite sin que él lo diga.
- [x] 1.3 Añadir la migración a la tabla de `supabase/README.md` con lo que hace y por qué actualiza por `(world_id, sort_order)`. **Y de paso, en ese mismo archivo:** «las **veintiuna** migraciones» → veintitrés, y los dos comandos de «Cómo aplicarlo» con **`npx`** —la CLI es dependencia del repo y no hay ninguna en el PATH, así que `supabase db push` a secas no corre—. Verificación: la tabla lista las 23 migraciones, la nueva dice que es de datos y no de esquema, y `command -v supabase` no devuelve nada mientras `node_modules/.bin/supabase` sí existe.

## 2. El comprobador del `config`, puro y con sus tests

- [x] 2.1 Crear `apps/web/src/game/levelConfig.ts` con la función que devuelve un `LevelConfig` válido o `null`, sin importar `three` ni Blockly, comprobando lo que fija el contrato §4.2: matriz no vacía, **todas las filas del mismo largo**, cada casilla de una de las tres clases, `start` y `goal` dentro del tablero, `facing` de las cuatro conocidas, `optimalSteps` entero mayor que cero, y **que la salida y la meta caigan sobre casillas que se pisan** —lo único que va más allá de la letra del contrato, y va porque es un error de siembra que sí se caza leyendo (`design.md`)—. Verificación: `npx tsc --noEmit` pasa y el módulo no aparece en el grafo de importaciones de `GameScene`.
- [x] 2.2 En ese mismo módulo, **una sola puerta para los tres campos de la fila**: recibe la versión, el `config` y el sobre tal y como vienen de la base y devuelve el nivel listo para jugar o `null`. La versión **del nivel** manda —si no es la conocida, se rechaza sin mirar nada más—, el sobre lo abre `openProgram`, y **un desacuerdo entre las dos versiones rechaza el nivel aunque las dos sean conocidas por separado** (`design.md`, «La versión del nivel manda…»). Verificación: la pantalla llama a una función y no a tres, y no combina resultados por su cuenta.
- [x] 2.3 Crear `apps/web/src/game/levelConfig.test.ts`. **Acepta:** el `config` del nivel 1 **leído del propio archivo `.sql` de la tarea 1.1** —el test importa la migración con `?raw` y saca el JSON de su `validation_rules`, no lleva ninguna copia del puzle escrita a mano—, y el tablero con muro y hueco de `debugLevel`, para que aceptar no dependa de que el tablero sea trivial; y el sobre con `workspace` vacío, que el §7 llama «sin programa de partida» y es justo el caso del nivel 1. **Rechaza:** el `{}` del valor por defecto de la columna; una casilla de clase desconocida; una fila más corta que las demás; una salida o una meta fuera del tablero y un `facing` que no sea de los cuatro; un `optimalSteps` de cero, negativo o no entero; **una salida sobre un muro y una meta sobre un hueco**; las `validation_rules` reales de un nivel sin rediseñar, copiadas de la 0012 (`{"goal":"choose_safe_path","requiresCondition":true}`); una versión desconocida; y **el desacuerdo entre las dos versiones**, que es el único caso que no se cae solo de comprobar cada campo por su lado. Verificación: `npm run test:run` pasa y el recuento sube por encima de los 169 de partida. **Y el acoplamiento al SQL se comprueba rompiéndolo**: cambiar el `optimalSteps` del archivo de migración tiene que poner el test en rojo; si sigue verde, la promesa de leer la migración es mentira y el caso no vale lo que dice valer.

## 3. La fila llega entera hasta la pantalla

- [x] 3.1 Ampliar `Level` en `apps/web/src/types/world.types.ts` con `narrative`, `starterCode` y `formatVersion` —los tres tipados contra `LevelRow`, no a mano—. Verificación: `npm run build` pasa sin tocar `database.types.ts`.
- [x] 3.2 En `apps/web/src/services/worlds.service.ts`, dejar de tirar esos tres campos en `mapLevelRow` y añadir la lectura de **un** nivel por su id, devolviendo `{ data, error }` con `AppError` y sin lanzar. Verificación: el servicio no interpreta `validation_rules` —lo pasa tal cual— y `npm run lint` sigue en cero avisos.

## 4. La lista de niveles, leyendo de la base

- [x] 4.1 Reescribir `apps/web/src/components/dashboard/student/StudentWorldLevelsModule.tsx`: fuera `LEVEL_TITLES` y fuera el `studentWorlds.find(...) ?? studentWorlds[0]`; el mundo se resuelve por su uuid contra las filas de `worlds` y los niveles salen de `levels` en su orden, con el título y la descripción de cada fila. Verificación: abrir un mundo real enseña sus tres niveles con sus títulos de la base, y ninguno de los diez inventados.
- [x] 4.2 Sacar completado / actual / bloqueado del progreso real, con el primer nivel del mundo siempre disponible, y no permitir entrar en uno bloqueado. Verificación: con una cuenta sin progreso, el nivel 1 aparece disponible y el 2 y el 3 bloqueados y sin abrirse al pulsarlos.
- [x] 4.3 Resolver el caso del mundo que no existe —decir que no está disponible y ofrecer volver al catálogo— y no anunciar el vacío mientras la consulta sigue en curso. Verificación: abrir `/dashboard/worlds/<uuid-inventado>` no enseña los niveles de ningún otro mundo.

## 5. La ruta y la pantalla de nivel

- [x] 5.1 Añadir la ruta del nivel a `apps/web/src/constants/routes.ts` y registrarla en `apps/web/src/router/AppRouter.tsx` con la misma guarda de rol que las demás del panel del niño. Verificación: la dirección existe y quien no sea `child` no llega a ella.
- [x] 5.2 En `apps/web/src/pages/Dashboard/Dashboard.tsx`, hacer que el colapso de `${ROUTES.WORLDS}/` deje de tragarse la pantalla nueva: dentro del caso de `WORLDS`, decidir `levelId ? nivel : worldId ? niveles : mundos`. Verificación: escribir la dirección de un nivel abre el nivel y no la lista de su mundo —que es el fallo que el comentario de `routes.ts` avisa y que no da ningún error—.
- [x] 5.3 Crear `apps/web/src/components/dashboard/student/StudentLevelModule.tsx` con la composición que el J6.3 ensayó en el laboratorio: el juego en la zona alta, la bandeja del lienzo superpuesta, y a la derecha la caja de bloques, los botones y las instrucciones. Las instrucciones salen de la **narrativa de la fila**, no escritas en el componente. Verificación: el texto que se lee en pantalla es el de la fila, y cambiarlo en la base lo cambia sin tocar código.
- [x] 5.4 Comprobar el nivel en esa pantalla antes de montar el juego, con la puerta única de la tarea 2.2, y si algo no cuadra **rechazar el nivel entero**, decírselo al niño y ofrecerle volver a la lista. El mensaje es **para un niño**: ni jerga, ni el JSON en pantalla, ni un error de consola por toda respuesta. Verificación: abrir un nivel sin rediseñar enseña ese mensaje, no dibuja tablero y **no descarga el trozo del motor 3D** —medido en la pestaña de red—.
- [x] 5.5 Dejar el laboratorio donde está y en desarrollo, y **pasarle el nivel que ahora es obligatorio**: `debugLevel`, que sigue vivo justo para eso. Es una decisión, no un tropiezo — `tsc` lo cazaría igual, pero entonces se arreglaría a la carrera. Verificación: `/dashboard/game` sigue abriendo el banco de pruebas con su rejilla 5 × 5 y con su `<pre>`, que es el instrumento **de esa pantalla**.

## 6. El juego se juega con el nivel que recibe

- [x] 6.1 Bajar el nivel por `apps/web/src/game/GameSceneLoader.tsx` junto al programa y los dos huecos, **sin importar `three` ni el comprobador**: sólo el tipo cruza. Verificación: el trozo principal del build no crece con nada del motor.
- [x] 6.2 En `apps/web/src/game/GameScene.tsx`, sustituir `const config = debugLevel;` por el nivel recibido y retirar el `import` de `debugLevel`. Verificación: `grep debugLevel apps/web/src/game/GameScene.tsx` no devuelve nada, y `debugLevel.ts` sigue existiendo para los tests y el laboratorio.
- [x] 6.3 Derivar el encuadre de partida del tamaño del tablero —la dirección de `CAMERA_START` escalada por el mayor de sus dos lados, con el suelo de `MIN_DISTANCE`, **y `BOARD_LIFT` escalado con ella**— sin cambiar el ángulo. Verificación: con la rejilla de pega 5 × 5 la vista de partida es **la misma de hoy**, y con el 1 × 4 el tablero se ve entero y sus cuatro casillas se cuentan sin tocar la cámara.
- [x] 6.4 Apartar `SCENERY` **sólo hacia fuera** cuando el tablero crece por encima de la huella de ±2,5 contra la que está escrito, y dejarlo donde está cuando el tablero es menor. Hacia dentro no: medido, acercarlo al borde de un 1 × 4 lo mete encima del tablero y deja la escena en blanco sin error en consola (`design.md`). Verificación: con 5 × 5 ninguna pieza se mueve —la diferencia es cero—, y con el 1 × 4 ninguna se mete en una casilla ni la tapa, mirado desde arriba **y desde el lado**.

## 7. Verificación de punta a punta

- [x] 7.1 Con la migración ya aplicada por el usuario, jugar el nivel 1 desde el panel: entrar al mundo, elegir el nivel, colocar `avanzar 3`, ejecutar y llegar a la meta. Verificación: la barra dice que se llegó con tres pasos, justo lo que cuesta la mejor solución.
- [x] 7.2 **Las dos señales del §4.10, sacadas de la pantalla de nivel y no del laboratorio.** El `<pre>` del banco de pruebas mide **otra** inyección de Blockly: puede publicar él y no publicar el de la pantalla de nivel, y entonces la comprobación sale en verde con el caso roto delante — que es exactamente cómo se pierde un fallo intermitente. La evidencia sale de la pantalla donde se juega: (a) que la barra **no** diga «No hay bloques que ejecutar» con los bloques puestos, que es el síntoma literal de que el editor de esa pantalla no publicó; y (b) que el **recorrido progrese** —el contador sube casilla a casilla y el personaje llega—, porque los frames de `requestAnimationFrame` descartan que Blockly no reparta y **no** descartan que la escena esté congelada a sesenta fps. Verificación: las dos se anotan por separado, medidas en esa pantalla, y `document.hidden` no vale como señal de ninguna de las dos.
- [x] 7.3 Recorrer el camino del §7 abriendo **a mano** la dirección del nivel 2, que sigue sembrado en el formato del juego anterior. Verificación: lo que se ve es un mensaje **para el niño** —ni jerga de desarrollador, ni volcado del JSON, ni pantalla en blanco— con la vuelta a la lista a mano, y ningún tablero dibujado a medias. Va con captura: es lo único de esta lista que no se ve solo.
- [x] 7.4 Dar el candado y el rechazo **por separado**, que son dos comprobaciones distintas: (a) en la rejilla del mundo, el 2 y el 3 salen bloqueados y no se abren al pulsarlos; (b) la ruta directa al 2 recorre el rechazo del §7. Verificación: las dos se enseñan con su propia evidencia, y (b) con la captura de la tarea 7.3 — que el 2 esté bloqueado en la lista **no** demuestra que la ruta directa se comporte.
- [x] 7.5 Capturas del tablero **desde arriba y desde el lado**, guardadas como PNG en una carpeta propia, mandadas al usuario con `SendUserFile` y **borradas antes de cerrar el cambio**. Verificación: en las dos se cuentan las cuatro casillas y se ve que el decorado no invade ninguna. Una captura de un ángulo no vale: desde arriba no se ven las rendijas ni lo que se mete por el lado.
- [x] 7.6 Ejecutar `npm run lint`, `npm run test:run` y `npm run build`, y anotar los números —tests, archivos, tamaño de los cuatro trozos y módulos transformados— contra los de partida: 169 tests en 19 archivos; 625,00 / 648,40 / 904,65 / 53,32 kB y 788 módulos. Verificación: lint en cero avisos, ningún test roto, y el crecimiento del trozo principal explicado por lo que entró y no por `three` ni Blockly.

## 8. Documentación y cierre

- [x] 8.1 Retirar las **tres** menciones vivas a un **J7.4** que ya no existe —se retiró al J13—: `apps/web/src/game/level.ts:56`, `apps/web/src/game/GameScene.tsx:406` y `apps/web/public/models/README.md:77`. **No se tocan** `docs/CONTEXT.md:2794`, que nombra al J7.4 para contar que se retiró —cambiarlo dejaría la frase diciendo que el J13 se retira al J13—, ni nada de `openspec/changes/archive/`, que es archivo, ni `apps/web/dist/`, que es salida de build. Verificación: `grep -rn "J7\.4" apps/web/src apps/web/public` no devuelve nada, y el `grep` sobre `docs/` sigue devolviendo la línea 2794 y sólo ésa.
- [x] 8.2 Actualizar `docs/CONTEXT.md`: §2.9 con la pantalla de nivel y el comprobador, §4.2b con el nivel 1 ya rediseñado y los ocho que no, §2.7 con la migración 23 y, en las prioridades, mover lo que este cambio cierra del P4. Verificación: la lista de archivos de §2.9 nombra las rutas reales de lo nuevo.
- [x] 8.3 Actualizar `docs/ROADMAP-JUEGO.md` §3 —J7.1 hecho— y `docs/ROADMAP.md` en lo que el paso 20 deja de deber. Y arreglar la lista corta de `xp_reward`, que está **en dos sitios** y en los dos lista **siete** valores para nueve filas: `docs/ROADMAP-JUEGO.md:416-417` y `docs/ROADMAP.md:466-467`. Los nueve son 100/120/140 · 180/200/240 · **150**/210/260; faltan el 150 y el 210 de la Costa de Bugs. Verificación: `grep -rn "240 y 260" docs/` no devuelve nada, y la lista nueva casa con la siembra de la 0012 contada fila a fila. `docs/CONTEXT.md` **no** repite esa lista —comprobado: su única aparición de `xp_reward` es la línea 1242, que habla de otra cosa—, así que ahí no hay nada que arreglar.
- [x] 8.4 Replicar en `openspec/config.yaml` lo que cambie de estructura, convenciones o prioridades, y comprobar con `npx openspec doctor` que el YAML sigue parseando. Verificación: `doctor` no se queja.
- [x] 8.5 Anotar en `docs/CONTEXT.md` §4 la deuda que deja el candado: **se comprueba en la lista y no en la pantalla de nivel**, así que escribir la dirección de un nivel bloqueado lo abre. Hoy lo tapa que los otros ocho sigan siendo ilegibles —y de ahí sale su única ventaja, que el camino del §7 se puede recorrer a mano—; **lo destapa el J7.2**, que al sembrar el nivel 2 deja que su dirección se salte el 1. Verificación: la entrada dice qué lo tapa y qué lo destapa, de modo que el J7.2 se lo encuentre en vez de heredarlo sin verlo.
- [ ] 8.6 Preparar el commit **enumerando las rutas** en `git add`, nunca `git add -A`, y pedir al usuario que lo autorice. Verificación: `git status` antes del commit enseña que lo que entra es sólo lo de este cambio.

## 9. Lo que el usuario pidió después, con la pantalla delante

- [x] 9.1 **Cero assets.** Reconstruir `GameScene.tsx` desde `7f0ad6f` con los cambios del J7.1 encima: fuera `Scenery`, `Obstacles`, `SCENERY`, `useLoader`, `Clone` y el `GLTFLoader`, y vuelta al cubo por casilla con el damero, el muro, la salida y la meta. Verificación: `grep` de `.glb`, `GLTFLoader` y `useLoader` en `apps/web/src/game/` no devuelve nada, y el trozo `GameScene` baja de 904,76 a **849,42 kB**.
- [x] 9.2 **Los 233 modelos se quedan**, decidido por el usuario, y `apps/web/public/models/README.md` lo dice con su porqué: nadie los carga desde el J7.1 y los hereda el J13. Verificación: quien encuentre la carpeta lee por qué no sobran, y que `public/` viaja entero al despliegue.
- [x] 9.3 **Las casillas son cubos y no losas.** Verificación: el `boxGeometry` mide `TILE_SIZE` en los tres ejes, se hunde media altura para que la cara de arriba siga siendo `y = 0`, y **no** se añade una segunda constante al lado de `TILE_SIZE`.
- [x] 9.4 **El tablero pasa a 5 × 1 con `optimalSteps` 4**, en una migración nueva —`202606030024_level_1_vertical_board.sql`, una sola columna— porque la 0023 está aplicada y no se edita. Resuelto otra vez a mano: `avanzar 4`, y por debajo de cuatro no hay programa. Verificación: la fila consultada contra la base da 5 filas × 1 columna, salida `{4,0}` al norte, meta `{0,0}` y `optimalSteps` 4.
- [x] 9.5 **El ángulo de la cámara lo eligió el usuario contra dos imágenes**: con el lateral (diagonal) y sin él (vertical de verdad). Eligió la diagonal, así que `CAMERA_START` se queda en `[6.7, 9.5, 9.5]` y queda escrito en `design.md` para que nadie se lo quite creyendo que arregla algo. Verificación: la decisión está en el diseño con lo que se probó y contra qué imagen se eligió.
- [x] 9.6 Jugar el nivel **en su pantalla** con el tablero nuevo. Verificación: «¡Perfecto! Llegaste a la meta con **4 pasos**, justo lo que cuesta la mejor solución» — el texto de la igualdad exacta, que sólo sale si el `optimalSteps` leído de la fila y el recuento del programa coinciden.
